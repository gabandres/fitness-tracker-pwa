{
  "name": "@macrolog/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared framework-free brain (domain types + pure math) for the Macronaut PWA and Expo app.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "sideEffects": false,
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
