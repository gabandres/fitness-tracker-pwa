// Moved to @macrolog/core (shared with the Expo app — see docs/adr/0012).
// This shim keeps existing `utils/weekly-budget` imports working.
export { computeWeeklyBudget, type WeeklyBudget, type DayBudgetBar } from '@macrolog/core';
