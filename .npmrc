# Angular 21 vs @angular/fire peer range is incompatible; our Angular 21
# stack ships first, and @angular/fire 20 is what's on npm. Prefer our
# pinned Angular 21 over the peer hint — functionally compatible, and
# this aligns local `npm ci`, GitHub Actions, and Firebase Cloud Build.
legacy-peer-deps=true
