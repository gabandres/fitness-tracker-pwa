rules_version = '2';

// No Storage features in use. Progress photos (ADR-0010) were removed; nothing
// reads or writes Storage now, so every path is explicitly denied.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
