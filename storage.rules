rules_version = '2';

// No Storage features in use. Progress photos (ADR-0010) were removed; nothing
// reads or writes Storage now, so everything is denied by default.
service firebase.storage {
  match /b/{bucket}/o {
    // No allow rules — all reads and writes are denied.
  }
}
