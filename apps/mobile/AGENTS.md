# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code (installed SDK is `expo@^57`; keep this URL in sync with `apps/mobile/package.json`).

# Entry point is NOT `expo-router/entry`

`package.json` `main` is a custom **`index.js`** at the app root. It imports
`expo-router/entry` for its side effect (routing behaves identically) and then
registers the Android home-screen widget's task handler — which must run at
module scope, before React mounts, because Android can wake the widget when the
UI was never started. Don't "fix" `main` back to `expo-router/entry`; it
silently kills the widget on Android. See `WIDGET.md`.

# `src/app/` holds ROUTES AND NOTHING ELSE — not even tests

Expo Router builds its route tree from a Metro `require.context` over
`src/app`, and [its regex excludes only `+api` and `+html`](https://docs.expo.dev/router/reference/testing/).
Every other file there is treated as a route **and bundled into the app**. A
colocated `*.test.tsx` therefore drags `@testing-library/react-native` into the
production bundle, which requires Node's `console`, which Metro cannot resolve:

```
Unable to resolve module console from @testing-library/react-native/dist/helpers/logger.js
```

Mobile tests live in **`src/__tests__/`** (or beside a non-route module, like
`src/components/SignInMethodsCard.test.tsx`) and import the screen through the
alias — `@/app/(app)/train`, never `./train`.

**`tsc --noEmit` and `jest` both pass while this is broken.** They do not run
Metro. On 2026-08-06 it took two EAS builds to the *Bundle JavaScript* phase
before anything noticed — a latent break from 2026-08-05, since no EAS build
had run in between. The cheap local gate is a real bundle, and it costs
nothing:

```sh
cd apps/mobile && npx expo export --platform android --output-dir <tmp>
```

**Run that before queueing any EAS build.** (Errored builds did not consume
plan quota in that incident — measured 7/15 before and after — but they cost
the queue wait, which on the Android free tier has run to two hours.)

**Measure the export before deleting it.** Nothing else in this repo can see
bundle size — not `tsc`, not the 340 jest tests, not Maestro, not the
fingerprint gate — and it is paid by every tester on every OTA download and
every cold start:

```sh
node scripts/perf-budget.mjs --platform android --export-dir <tmp>
```

The bundle went **11,095,849 → 13,140,531 bytes (+2.0 MB, +18%)** in the single
publish that shipped the food index, and drifted another **142,341** in the day
after with nobody noticing — which is how it was found. Baselines live in
`scripts/perf-budget.json` and a breach is either a regression to find or a
growth to accept on the record (`npm run perf:budget -- --update`, and say why
in the commit message). Raising `headroomPct` to make it pass defeats the point.

# An OTA update that misses everyone looks EXACTLY like one that worked

This app uses **EAS Update**. A JS-only change ships with `eas update` and needs
no build — but delivery is gated on `runtimeVersion`, which is the
**`fingerprint` policy** (`app.json`), derived from the native dependency graph.

**If the fingerprint changed, `eas update` still succeeds.** It publishes under a
*new* runtime version that no installed binary matches, so every tester silently
stays on old code. Nothing errors. There is no warning. The only signal is that
the bug you "fixed" keeps getting reported.

## The fingerprint is a property of the MACHINE, not just the commit

**Run the gate — and `eas update` itself — on the machine that BUILDS that
platform. Since 2026-08-17 that is not one machine:**

| Platform | Built on | Gate + `eas update` from |
|---|---|---|
| **Android** | the Windows workstation (vc 31+) | **Windows** |
| **iOS** | `ignia-mac` | **`ignia-mac`** |

**Bare `eas update` publishes BOTH platforms and is therefore correct on
NEITHER machine — always pass `--platform`.** `.claude/hooks/guard_eas_update.py`
enforces the whole table; it blocks the bare form everywhere and blocks each
platform from the wrong host.

The rule below is why, and it still holds — only the "which machine" answer
changed. The same commit fingerprints differently on the two machines:

| Machine | commit `c3a7333a`, android | ios |
|---|---|---|
| **`ignia-mac`** | `5758fe4f…` | `6c756c19…` |
| Windows workstation | `c0b85c15…` | `781be0c8…` |

Three commit-independent causes, found by diffing the two `sources` arrays
(516 entries on Windows, 286 on the Mac):

- ~~a stale **`apps/mobile/android/`** prebuild dir exists on Windows only — it is
  gitignored, so nothing syncs or removes it, and `dir:android` is hashed;~~
  **DISPROVEN 2026-08-17 — `dir:android` is listed as a source but contributes
  NOTHING to the hash.** Measured on the Windows workstation by generating with
  the directory present and again with it moved aside: `3d3bc410…` both times,
  521 sources vs 520. Almost certainly because `android/` is wholly gitignored
  and the fingerprinter honours that. Two builds from *different* `android/`
  contents (a stale vc-10 prebuild and a fresh vc-31 one, differing in
  versionCode, AndroidManifest.xml and gradle.properties) also embedded the
  identical fingerprint. So this is not a cause of Windows/Mac divergence, and
  deleting `android/` before a gate run is a no-op. **The general rule, measured
  2026-08-17: a `dir:` source hashes only git-TRACKED content.** Confirmed a
  second way on `dir:modules/quick-add-tile/android`, which unlike `android/` is
  a tracked directory: its 299-file untracked Gradle `build/` output was moved
  aside and the hash stayed `3d3bc410…` at 521 sources. So Gradle output sitting
  inside a hashed directory cannot strand an OTA, and neither can any other
  untracked file — "is it gitignored" was the narrower version of this;
- **CRLF vs LF** in tracked files — measured 2026-08-17 and it is **exactly two
  files**, `.gitignore` and `targets/widget/expo-target.config.js`, which are
  CRLF in the Windows worktree and LF on the Mac. `.gitattributes` already says
  `* text=auto eol=lf`, and the index is LF throughout; these two simply predate
  that file and git does not re-checkout on an attribute change. **Fixing them is
  a worktree-only act — no commit — but it MOVES the Windows fingerprint**, so do
  it in the same change as an SDK bump or another native change that moves it
  anyway, never on its own;
- ~~divergent `node_modules`, which changes which config-plugin files are walked.~~
  **DISPROVEN 2026-08-17.** The two trees are not divergent. The Windows iOS
  `sources` array carries **228 entries the Mac's does not** — every one of them a
  transitive dependency of a config plugin under a *nested* `node_modules`
  (`@bacons/apple-targets/node_modules/@expo/plist/…`, `@bacons/xcode/node_modules/xmlbuilder/…`).
  **All 228 files were then confirmed present on the Mac** (`present=228
  missing=0`), with identical `@expo/fingerprint` 0.15.5, identical
  `@bacons/apple-targets` 5.0.0 and a byte-identical `package-lock.json` that
  itself prescribes those nested paths. The Mac's array contains **zero** sources
  Windows lacks. So this is not an install difference: `@expo/fingerprint` walks
  the config-plugin require graph *across* nested package boundaries on Windows
  and stops at them on macOS. Aligning Node/npm cannot converge the two hashes,
  and `npm ci` is not a fix for a mismatch.

**Consequence: cross-host fingerprint parity is not achievable and is no longer a
goal.** It also does not need to be. Since 2026-08-17 each platform is built,
gated and published on one host — Android on Windows, iOS on the Mac — so each
hash only ever has to match binaries produced by the same machine, which it does:
measured that day, Windows/Android returns `3d3bc410…` (= live vc 31, read from
the `.aab`) and Mac/iOS returns `886bf0b3…` (= build 55, read from the `.ipa`).
**A hash is valid only against artifacts from the machine that produced it**;
comparing across hosts is meaningless, not merely inconvenient.

The corollary is a live hazard: because the 228-entry gap is `@expo/fingerprint`
behaviour rather than repo state, **an upgrade of that package can move the
Windows hash with no change to this repo at all** — silently stranding every
Android OTA. Re-read the gate against the shipped `.aab` after any bump of
`expo-updates`/`@expo/fingerprint`, not just after a code change.

**Ground truth is inside the artifact — read it, don't compute it:**

```sh
unzip -p build-<ts>.aab base/assets/fingerprint          # Android
unzip -o -q build-<ts>.ipa -d /tmp/ipax && \
  cat /tmp/ipax/Payload/*.app/EXUpdates.bundle/fingerprint   # iOS
```

`Expo.plist` only says `EXUpdatesRuntimeVersion = file:fingerprint`; the value
is in that file. Verified 2026-08-07: vc 13's `.aab` holds `5758fe4f…` and
build 25's `.ipa` holds `6c756c19…` — the Mac's numbers, not this machine's.

**This already cost three updates.** Every OTA published on 2026-08-07 before
22:00 went out under `c0b85c15…`/`781be0c8…` (confirmed with
`eas update:list`), which is the *Windows* fingerprint and matches neither
verified binary. The "fleet split across two runtime versions" written up that
day was largely this artifact: one machine's number was being compared against
another machine's. The 22:00 update is the first one published from the Mac and
the first that provably matches a shipped binary.

So the gate before every publish runs **on that platform's build host** — iOS on
the Mac, Android on Windows:

```sh
# iOS — on ignia-mac
ssh ignia-mac "cd ~/fitness-tracker-pwa && git checkout main && git pull --ff-only"
ssh ignia-mac "cd ~/fitness-tracker-pwa/apps/mobile && npx expo-updates fingerprint:generate --platform ios"

# Android — on the Windows workstation
cd apps/mobile && npx expo-updates fingerprint:generate --platform android
```

Compare the `hash` against the fingerprint read out of the binary testers are
running. **Same → the update lands. Different → it reaches nobody and you need a
build.** Note `node_modules` is an input: an `npm install` on the build host can
move the fingerprint away from an already-shipped binary, so check the gate
*before* installing, not after.

~~**Generate it AFTER `expo prebuild`, or it does not match the artifact.**~~
**WITHDRAWN the same day — this rule was a misreading.** It was inferred from one
observation: with a stale `android/` on disk the CLI returned `5621a4fa…`, and
after a prebuild it returned `d8741525…`, matching the AAB. Prebuild was the
visible act in between, so it got the blame.

The vc 33 build then measured the gate **before and after `expo prebuild` and got
`c1c010ac…` both times.** Prebuild does not move the hash. What moved it in the
original observation was `apps/mobile/.gitignore` flipping between CRLF and LF —
see the line-endings section at the end of this file, which reproduces both values
on demand by touching nothing else.

Keep the underlying habit for a better reason: **read the fingerprint out of the
artifact, never compute it.** A computed hash can disagree with a shipped binary
for reasons that leave `git status` clean, and the artifact is the only value
that cannot.

Fingerprints of the binaries carrying `expo-updates` (update these when new ones
ship):

| Platform | Binary | Fingerprint | Source | Note |
|---|---|---|---|---|
| Android + iOS | **Android OTA 83 on vc 37 / iOS OTA 50 on build 60** (2026-08-28) | `ae526937893adb7f7349321b05caf2732da9658b` / `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | **A fast you logged wrong was a fast you were stuck with — ADR-0032 decision 3, open since #97** (`1256a43c`). Android `01a04b54-12b2-73d5-8a87-333adc281d48` (group `952ab67f`), iOS `01a04b54-1f39-7bec-94ca-8f84ea480d53` (group `4e7298e8`), both confirmed with `eas update:list` rather than off the log. A completed fast can be corrected or deleted from the History day detail, a fast nobody timed can be logged there, and Today's fasting row now opens the same sheet to correct a RUNNING fast's start — `startFast` has always accepted a `startedAt` on both adapters and **no UI ever passed one**. Every write on these paths is `source: 'manual'`, and a corrected fast is demoted from `'timer'`. **The control was decided by the fingerprint, not by taste:** `@react-native-community/datetimepicker` is a NATIVE module, so adopting a date picker would move the hash and no later fix to this screen could reach a user by OTA — the editor is six nudge buttons (`−1d −1h −15m +15m +1h +1d`) against a selected field with the duration live above them. **That is also why overlap is REFUSED rather than greyed out** as the ADR specifies: a nudge row has no surface to grey. `overlappingFasts` is shared pure code, half-open so back-to-back fasts stay legal, and it is a UX guard and NOT a security one — `firestore.rules` sees one document and cannot query its siblings, so overlap is unenforceable there by construction. Detection is best-effort within the day listener's ±3-day window. ADR-0032 gains Amendment 1 recording both divergences. **NO RULES DEPLOY, and that was checked rather than assumed** — `match /fasts/{fastId}` already allowed update/delete and `isValidFast` already enumerated `manual`; `firestore.rules` is untouched. `updateFast` uses `setDoc` with no merge option deliberately: `isValidFast` is `hasOnly` over three fields and validates the POST-merge document, so a partial update would leave a stale `'timer'` behind. `WHATS_NEW_VERSION` **bumped** to `2026-08-28-fix-a-fast` in all three locales — an OTA has no store listing, so the banner is the only announcement a visible feature gets. Suites: core 1391, mobile 557 (+23), web 206; all four build units green. Export + `perf-budget`: Android **+2.79%**, iOS **+2.73%**, assets unchanged on both, within budget. **NOT device-verified on either platform** — the LG G6 was not driven for this. The risk that buys is layout, which RNTL cannot see: the sheet is a new control with six pills in one row at 360 dp. **One harness fact worth keeping:** under React 19 the state update a press schedules is not committed when `fireEvent.press` returns, so a synchronous assertion reads the PREVIOUS render — measured while writing `fast-sheet.test.tsx`, where the same nudge reads the old duration synchronously and the new one after a flush. |
| iOS | **iOS OTA 49 on build 60 - the rollback REVERSED on the owner's call** (2026-08-28) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | `01a04ad6-d302-7580-94ed-fd680d736a20` (group `53a6137d`), from `f11766e3` - the same JS as Android OTA 82, which is device-verified. **Re-shipped deliberately to obtain a reproduction**, after the rollback on the row below. The owner chose to test it rather than wait for Apple to aggregate the crash report, which is a legitimate trade: one crash on one device with no telemetry is not proof the OTA is bad, and the alternative was leaving two fixes off iOS indefinitely on a single unreproduced event. **THE DECISION TREE THIS BUYS, and it is the reason re-shipping is worth anything:** if it crashes on the RESTART and the app is then fine, the fault is in the reload path and NOT in `motion.tsx` - a broken hero would crash every time Today rendered, not once on apply. If Today crashes on every open, `motion.tsx` is the culprit and the ring change comes out. If it does not crash at all, the first event was transient and the rollback was the correct precaution rather than a correct diagnosis. **Do not record any of those as the answer until one of them is observed.** |
| iOS | **ROLLBACK on build 60 - iOS OTA 47 and 48 are NO LONGER SERVED** (2026-08-28) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update:republish` output | **The owner tapped Restart on an iOS OTA and the app CRASHED**, hard enough that iOS showed its share-with-developer modal on the next launch (so it did relaunch - not a crash loop). Rolled back by republishing OTA 46's group `2318d259` as `5cadc8e7` / `01a04a4a-83fe-7e10-9279-fe411e035700`, which is the exact JS the owner had confirmed running earlier the same day (bundle `01a047dd`). **ANDROID IS UNAFFECTED and keeps both fixes** - OTA 82 is device-verified on the LG G6. **What is known:** nothing in Sentry (newest `ignia-mobile` event is 08-26), and ASC returns **0 diagnosticSignatures** for build 60 because Apple aggregates those with a delay. A native modal plus no Sentry event means it died BELOW the JS layer. **What is ruled out:** `textAlignVertical`, which OTA 81 added to `motion.tsx` and which is Android-only - it fit the Android-works/iOS-crashes split perfectly and is WRONG, because that prop already ships on iOS in six other places (`EntrySheet`, `scan`, `coach`, `feedback`, `MealText`, `train-styles`). **What is narrowed:** OTA 82's diff is pure JS state logic and can hang a spinner but cannot produce a native crash, so the frame is OTA 81 (`motion.tsx`), the reload path itself, or something unrelated - this project has prior `EXC_BAD_ACCESS` and `WatchdogTermination` on record. **Do NOT re-ship on the assumption it was transient, and do NOT conclude the OTA is fine because the app works after a rollback** - those are different states. One crash on one device with no telemetry is also not proof the OTA is bad; the same discipline `CLAUDE.local.md` applies to the single Google Sign-In code-8 event applies here. Re-read `diagnosticSignatures` on build 60 once Apple aggregates, or get the `bundle` id off the owner's Settings screen, before deciding. |
| Android + iOS | **Android OTA 82 on vc 37 / iOS OTA 48 on build 60** (2026-08-28) | `ae526937893adb7f7349321b05caf2732da9658b` / `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | **Today drew a hero belonging to NOBODY before the profile landed** (`86f72754`) - the second half of the ring report and the more serious half, since OTA 81's clipping was cosmetic and this is a wrong calorie target. Android `01a04a28-25f5-7c6a-86e6-2a582ba3bf9c` (group `e49324cf`), iOS `01a04a2a-8279-70a0-97c1-fdd7d505df56` (group `c208e759`). **`snapshotArrived` is latched by the LOGS listener alone**, so the spinner cleared while `profile` was still null - and `dailyTargets(null, ...)` does not decline to answer, it returns a SEED. Measured: **calorieTarget 1800, proteinTarget 0**. Today painted a full hero from those, then count-upped to the real target once the profile arrived, sweeping the big number through values that were never true. **That sweep is why the clipped 1,14 was visible at all** - at rest the box is always sized correctly, so the defect looks fine every time anyone checks. **`loading` now also requires the profile, and the ASYMMETRY is the design:** `logsReady` takes an authoritative snapshot OR a disk-cache hit, while `profileReady` takes ANY answer including a cache-only one. Requiring an authoritative profile would hang a cold-cache offline start forever - the exact trade the existing note says was rejected for logs, and the failure OTA 59 shipped and had to correct on a device. A brand-new account settles too: null is that user's correct answer. The rule moved to a dependency-free `today-gate.ts` and is tested there, the same seam `health-errors.ts` got the same day and for the same reason - `useToday` reaches Firestore through `ledger.ts`, so `requireActual` pulls Firebase's ESM build through a transform jest cannot handle. Five cases, including the no-hang guarantee asserted across ALL four ready/failed combinations. **VERIFIED ON DEVICE against the published bundle** (LG G6): at 3 s into a cold start the screen is the brand loader with **no hero and no numbers** - the gate holding - and it settles directly on **2,291 kcal left / 0 of 2,291 / 145g protein / maintenance 2,691**, the account's real values, with no sweep. **The protein target reading 145g rather than 0g is the tell** that the seed defaults never rendered. `WHATS_NEW_VERSION` not bumped - a bug fix. Export + `perf-budget`: **+2.58%**, assets unchanged. |
| Android + iOS | **Android OTA 81 on vc 37 / iOS OTA 47 on build 60** (2026-08-28) | `ae526937893adb7f7349321b05caf2732da9658b` / `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | **The Today ring could drop a digit, and it was a FROZEN LAYOUT, not a formatter** (`2f2777dc`). Android `01a04954-8a96-7f1d-9be3-32bdc1b1d25c` (group `feeed399`), iOS `01a04957-5b6a-7898-8805-ac5b71033c42` (group `3cd9e6c7`). Reported from an owner screenshot: the hero read **1,14**, one glyph short, while the legend under it read 1,542 / 2,003 - a remaining of **461**. **`formatNumber` was exonerated by reading it** (1141 -> 1,141, correct grouping, no truncation), which matters because the symptom points straight at the formatter and that is an hour lost. **`CountUpText` renders through an animated TextInput, and Yoga measures that box ONCE on the JS pass from React-visible props while `animatedProps` writes `text` on the UI thread, where layout never re-runs.** The width is therefore frozen at whatever string React last rendered, and every wider string the animation passes through is CLIPPED - a box measured for a 3-digit 461 leaves exactly 1,14 of 1,141. `styles.countUp` set no width and **no call site sets one, so all six are latent**; the hero surfaced it first because it is `position: absolute` + `alignItems: center` (free-sized to intrinsic content), largest font, widest value. **Fix: a hidden `<Text>` carrying the widest string the animation will show, with the input laid over it** - the ghost is the only thing Yoga sees, so it cannot be clipped. A fixed `minWidth` was rejected: the right value depends on font, locale separators and magnitude, and a wrong one stays invisible until a user crosses a digit boundary, which is how this survived. **NO TEST HERE COULD HAVE CAUGHT IT** - RNTL runs no Yoga pass, the same blind spot behind the F-series defects and OTA 74's unflexed row - and there was no coverage of `CountUpText` or `HeroRings` at all, so `widestCountUpText` is extracted pure and pinned by 8 cases (both directions, the thousands crossing, pt-BR separators). The test mocks `@/lib/auth` per-file rather than moving the helper to its own module, **deliberately: that would drag `formatNumber` out of `motion.tsx` and it is a worklet.** **VERIFIED ON DEVICE against the published bundle** (LG G6): `DownloadComplete`, then `CheckCompleteUnavailable` + `NoUpdatesAvailable` on the next launch, and the ring renders **2,291** - five glyphs, complete, centred, no ghost visible, vertical alignment intact. **A SECOND DEFECT IS DIAGNOSED AND NOT FIXED:** the ring should never have shown a 4-digit number at all. `loading` releases as soon as the OFFLINE CACHE paints (`!snapshotArrived && !logsFromCache`), by design, so the hero mounts on cached numbers and then animates to the live snapshot - through values that were never true. The flare effect ten lines above it already guards this class with a null-first ref; the count-up does not. `WHATS_NEW_VERSION` not bumped - a bug fix. Export + `perf-budget`: **+2.57%**, 326 bytes SMALLER than the shipped bundle. |
| Android + iOS | **Android OTA 80 on vc 37 · iOS OTA 46 on build 60** (2026-08-28) | `ae526937893adb7f7349321b05caf2732da9658b` / `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | **Two Sentry signatures that were device conditions, not crashes** (`838d79e6`). Android `01a047da-3285-7085-8468-6ca42af5d64b` (group `f7f1826d…`), iOS `01a047dd-0baa-7d0e-85d5-488dc659c53a` (group `2318d259…`). **(1) `app-update.ts` left the FALLBACK `openURL`'s rejection unhandled** — the pattern `52f7b3be` fixed for Settings links, reached through a door that fix did not cover. Sentry reports an unhandled rejection exactly like a crash, and the user gets a button that does nothing. **It matters more here than anywhere else:** the audience for an *update available* banner is by definition on an old build, which on Android is often an ORPHAN RUNTIME no OTA reaches — so that button is their only route forward and it was the one that broke. The still-open `IGNIA-MOBILE-8` event of 2026-08-25 is release 1.2.0 / **build 49**, exactly such a user; that is why the signature stayed open and is NOT evidence the earlier fix failed. **(2) HealthKit `Code=5` "Authorization not determined" was RE-THROWN** on every foreground (`IGNIA-MOBILE-H`, on live build 60). It means the local `connected` flag says yes while iOS holds no grant — a reinstall, a restore from backup, or a revoke in Settings → Privacy → Health: AsyncStorage survives, the grant does not. Same class as the `Code=6` already swallowed, so `isLockedDeviceError` became `isExpectedHealthState`. **Deliberately NOT done: flipping `connected` to false on a Code=5** — a transient "not determined" during a restore would silently disconnect a working account, and this runs on every foreground; offering a reconnect is a UI decision, not a catch-block one. The predicate moved to a dependency-free `health-errors.ts` because `jest.setup.js` mocks `health-sync` globally and `requireActual` pulls Firebase's ESM build through a transform that cannot handle it — a pure predicate deciding whether a user generates a crash report needs a seam a test can reach. **`WHATS_NEW_VERSION` deliberately NOT bumped** — these are bug fixes, and a bump re-fires the banner at everyone who dismissed `2026-08-27-mobility` hours earlier; same call OTA 78 made. Both fingerprints re-verified on their own hosts before publishing. Export + `perf-budget`: Android **+2.58%**, iOS **+2.52%**, both within budget. |
| Android | **vc 39 — RELEASED to INTERNAL testing 2026-08-27, and it is what BROKE the health-apps deadlock** | `ae526937893adb7f7349321b05caf2732da9658b` | **read from the `.aab`** | **The deadlock recorded on the vc 38 row below was a DETECTION problem, not a policy catch-22, and the ordering is the whole trick.** Three attempts, in one sitting: (1) direct androidpublisher with `status: 'draft'` **commits** — but Play never re-scans, so `READ_EXERCISE` stays invisible to the declaration; (2) the same call with `status: 'completed'` returns **403 PERMISSION_DENIED, "You must let us know whether your app includes any health features"** — from `androidpublisher` itself, no fastlane involved; (3) **publishing that same bundle to INTERNAL testing through the Play Console web UI succeeds**, and THAT is what makes Play scan it. `android.permission.health.READ_EXERCISE` then appeared in the declaration's detected list and became declarable. **So the route is: release to a track Play does not gate (internal), let it detect, then declare.** A draft is not enough and the API cannot get past its own gate. **Do NOT re-derive this from the fastlane issue** (#27960, "specific to Google API uploads via fastlane") — it is a red herring here; the 403 was reproduced with a hand-rolled `edits.insert` → `bundles.upload` → `tracks.update` → `commit`. Internal was chosen because it takes **no testers**, so the release reaches nobody: the only warning on the review page was *"This release will not be available to any users because you haven't specified any testers"*, which is the desired state. **alpha was NOT touched — it still reads vc 37 `completed`**, so the production-access application (closed testing, 12 testers / 14 days) is undisturbed. vc 39 embeds `ae526937…` = live vc 37, so nobody is stranded. Declaration submitted for review the same day (Step 1 needed no change; Step 2's *Activity* group was 2/3 and now carries a 914-char `READ_EXERCISE` justification; Step 3 required nothing). **When it clears review the API path should stop 403ing, which is what makes vc 40 scriptable** and unblocks Zero-Tap, R8 reaching users, and the Oura Health Connect read together. |
| Android + iOS | **Android OTA 79 on vc 37 · iOS OTA 45 on build 60** (2026-08-27) | `ae526937893adb7f7349321b05caf2732da9658b` / `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | **The Mobility creation chip — the gap OTA 78 shipped with, closed the same evening.** Android `01a04514-5845-7266-aca8-2a1d92e6d015` (group `7e4aae2f…`), iOS `01a0451a-6380-7033-95d0-afca39e262a7` (group `a8198bcf…`), both from `67193489`. **The bug was not a missing label.** `appendEx` scaffolded three `working` sets whatever log style was picked, so a stretch typed BY HAND came out `logStyle: 'time'` with `kind: 'working'` — able to take a `maxDurationSec` PR and invisible to the dose note. The seeded path was always correct; only the hand path was not. **A fourth chip, not a smarter default, and the rejected option is the point:** defaulting `time` -> `mobility` guesses, and BOTH its failure directions are silent — a mis-tagged stretch takes a phantom PR, a mis-tagged plank silently stops earning one. A test asserts both directions so a later "simplification" to a default fails rather than passes. **THERE WERE TWO DOORS.** `addExerciseToActive` builds `newWorkoutSet()` the same way, so adding a stretch to a LIVE session had the identical defect; `newWorkoutSet` now takes a kind and that sheet carries the same chip. The exercise EDIT sheet deliberately keeps three chips — it changes a catalog log style and creates no sets. Re-adding a seeded stretch from the catalog is covered by `MOBILITY_SEED_KEYS`, derived from the library rather than listed twice. **Layout was measured, not eyeballed:** four chips do not fit one row at 360 dp — "Weight x reps" alone is wider than the ~76 dp each would get — so the FOUR-chip rows wrap to a 2x2 grid and the three-chip rows keep their single row, because a 47% basis would break those into a 2+1. **VERIFIED ON DEVICE against the published bundle** (LG G6): the 2x2 grid renders with every label on one line, Mobility fills correctly when selected, and creating "Pigeon Pose" through it produced — read back out of PROD Firestore — a template exercise `{logStyle: 'time', sets: [mobility, mobility, mobility]}` and a catalog doc `{logStyle: 'time', seedKey: null}`, correctly NOT seeded. `WHATS_NEW_VERSION` bumped to `2026-08-27-mobility` — the owner's explicit call that a new way to log a whole category of work earns a re-fire where the day's bug fixes did not. Copy leads with mobility in all three locales and still claims nothing about soreness, recovery or injury. Export + `perf-budget`: **+2.57% raw**, unchanged from OTA 78, within budget. |
| Android + iOS | **Android OTA 78 on vc 37 · iOS OTA 44 on build 60** (2026-08-27) | `ae526937893adb7f7349321b05caf2732da9658b` / `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | **ADR-0028 mobility — the stretching feature, shipped as a correction to a capability that already existed.** Android `01a044ec-b2aa-7af5-8d5e-3bb463283df0` (group `75daaf44…`), iOS `01a044fd-4d21-7125-aee5-8c2c06a20e9e` (group `377aaead…`), both from `9e59f6e0`. `SetKind` gains `'mobility'` in FOUR mirrored sites, `isWorkingSet` excludes it, `packages/core/src/mobility.ts` flags a pre-lift hold over 60 s. **VERIFIED ON DEVICE against the published Android bundle** (LG G6, sideloaded vc 39, which embeds the same `ae526937…` runtime): the *Update ready* banner fired, Restart applied it, `Mobility Reset` appears in Starter templates as **8 exercises · 14 sets**, and cloning it wrote — read back out of PROD Firestore, not off a screen — eight catalog rows with `logStyle: 'time'`, `progression: null` on every one, and 14 planned sets each `{kind: 'mobility', durationSec: 45}`. **The guardrail was exercised in all three directions on the device**, which is what a unit test cannot show: at 45 s no note on any of the eight; pushing Cat-Cow to 90 s with a lift after it raised the note and **left the 90 in the field** (it warns, it does not cap); removing that lift made the note vanish while the hold stayed 90 s, which is amendment 1B's mobility-only case — "not applicable", not "safe". **Firestore accepted every write with no rules deploy**, confirming ADR-0028's claim that `isValidWorkoutSession` never reaches set-level `kind`. `WHATS_NEW_VERSION` deliberately **not** re-bumped — the copy was extended instead, on the same-day precedent from OTA 77, because a bump re-fires the banner at everyone who dismissed it hours earlier. Export + `perf-budget` re-run: **+2.57% raw**, within budget, of which mobility is ~9 KB. **One gap this shipped WITH, found by the owner reading the result rather than the diff:** adding a timed exercise by hand still creates `working` sets (`TemplateEditorModal.tsx:325` hardcodes three), so a hand-authored stretch is `logStyle: 'time'` with `kind: 'working'` — it can still take a `maxDurationSec` PR and the dose note never fires on it. The seeded path is correct; the hand path is not. |
| Android | **OTA numbers 75–77 on vc 37** (2026-08-27) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **Meal-text is sized by the food instead of by a fallback.** **75** `bc57dbc6-0a73-4c28-9a13-99c7090e5c03` — units resolve through a volumetric row the food itself carries, plus the "We read: …" echo. **76** `ff6c771c-d372-43ef-9ebf-b5e22721e30b` — one unfamiliar word no longer empties the result. **77** `8bf52f7c-ac7f-411e-9618-7799e5ad38dd` — cooked-not-raw for eating units (#104), clearer assumed copy (#105), Portuguese units + es/pt food names (#106). **The bug class is what to remember: every failure produced a plausible NUMBER rather than an error, so none of it was visible.** `250 ml milk` logged 25,000 g / 15,250 kcal because `ml` was in `UNIT_MAP`, in no gram table, and matched no label — so it fell to the per-100 g branch, which multiplies 100 g by the QUANTITY. **Two dead ends worth not repeating**, both in the commit messages: dropping words the index does not contain fails (`pure` lives inside `puree`), and scoring absent words with a penalty fails too (`headMatchBonus` is worth hundreds, so "Butter, tub" scored 91.7 against "Peanut butter" at 41.0). Dropping the word and re-ranking with the untouched scorer is what works. **A chain guard was needed on top**: the relax pass made `starbucks latte` return generic Coffee, Latte rows, which the absence test in `functions/test/usda-db.spec.ts` exists to forbid — a generic latte presented as a Starbucks answer is fabricated health data. **VERIFIED ON DEVICE against the published bundles** on the LG G6, both themes: `1 tsp honey, 250 ml milk, 2 slices whole wheat bread` → 21 / 157 / 182 kcal, written to Firestore and read back in Entries as Breakfast 360 kcal; `1 teaspoon of pure honey` → 21 kcal (the reported bug); `1 xicara de arroz branco` → **1 cup, cooked (174 g) 167 kcal**, proving #104 and #106 in one row; a six-modifier utterance truncating cleanly at 360×720 dp. `searchFoods` was **redeployed** the same day — the ranker is mirrored there and the web PWA still calls it. Gate `ae526937…` = vc 37 each time; export + `perf-budget` re-run, +2.50% raw, within budget. `WHATS_NEW_VERSION` bumped ONCE to `2026-08-27-your-words`; the third publish extended that copy without re-bumping, because re-firing nags whoever just dismissed it. |
| iOS | **OTA numbers 41–43 on build 60** (2026-08-27) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | The iOS halves of Android 75–77, published from `ignia-mac`: `39c47710-a549-4609-8c58-cfaf4c51d705`, `daa89846-5e73-4721-8a69-d0fde511e443`, `c539c87a-c564-404e-9a0a-a3dcbb3082b5`. **Reaches the PUBLIC App Store** — build 60 is `READY_FOR_SALE`, so the blast radius is every iOS user. Gate run ON THE MAC and parsed as JSON each time = `7b347b0f…` = build 60; exports run there; +2.38% raw / +2.55% gzipped, assets unchanged, within budget. **Unusually well corroborated for an iOS publish:** the owner reported the `pure honey` failure from their own iPhone within minutes of OTA 41 landing — which is simultaneously the bug report and proof the update reached the public App Store. The identical JS is device-verified on Android in detail above. |
| Android + iOS | **Android OTA 73–74 on vc 37 · iOS OTA 40 on build 60** (2026-08-26) | `ae526937…` / `7b347b0f…` | `eas update` output | **#103 — the Trends stub rows can be dismissed, which is what shipped INSTEAD of "My View".** Android `01a040ea…` then `01a040ef…` (the layout fix), iOS `12dac28e-b01e-4530-88db-5b8194a50542` (`01a040f5…`, from `d474865a`). **The reasoning is the reusable part:** ADR-0034 refused a show/hide catalogue and named the strongest objection to its own decision in the same breath — a user with no wearable has a permanently unhideable sleep stub row. After consolidation Trends is four elements and every card already self-gates, so the ONLY thing a user cannot get rid of is a row whose whole content is "you have no data". That row got a dismiss; the catalogue did not get built. **It dismisses the STUB, never the card** — the flag is read only in the empty state, so the card returns the moment there is evidence, which is what makes a one-way dismiss safe with no restore affordance to explain. **VERIFIED ON DEVICE, all four claims**: the control renders inset, tapping it removes the row with no residue, the dismissal survives a force-stop cold start, and **the card came back when the fasts were restored despite the stub still being dismissed**. **One real defect the specs could not see:** `linkRow` had no `flex`, so inside the new row container it took its intrinsic width and shoved the X hard against the screen edge, outside the body's own padding — 18 green specs for that card, RNTL runs no Yoga pass. That is OTA 74. |
| Android | **OTA numbers 71–72 on vc 37** (2026-08-26) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **Trends consolidated into two panels, and the tab choice is remembered.** **71** is `fac4a352-591e-4838-a69b-dbb7fe93d1da` — Weekly (This week ⇄ Budget) and Habits (Sleep ⇄ Fasting), taking Trends from six elements to four. **72** is `7a4ede8a-c63e-4869-bac4-0d5f189d28b5` (from `dc689e20`) — the tab choice persists per device. **This is ADR-0034's own advice followed in order: consolidation BEFORE configuration.** The Habits merge only became visible once fasting shipped hours earlier — both faces draw a fourteen-column strip with a median line and a headline duration, so stacked they read as one chart rendered twice. **The tab strip sits in the slot the uppercase section label occupied**, which is what keeps the page rhythm intact: nothing is added above the fold, one thing is removed. **The Habits strip only renders when BOTH faces have a card** — a tab leading to a stub row promises something it cannot show. **Persistence is AsyncStorage, and the distinction from ADR-0034 option C is the whole cost of the feature:** option C stores hidden-card ids on the PROFILE, which needs a `firestore.rules` deploy, and `hasOnly` is evaluated against the merged document so that deploy is cross-frontend and can start rejecting the FROZEN web's profile writes. A device-local cache needs none of it. If it ever has to sync, it has become option C and must be priced as option C. **VERIFIED ON DEVICE, both themes**: both panels, both faces, and the choice surviving a tab change AND a full force-stop cold start with no flash through the default. Gate `ae526937…` = vc 37 each time; export + `perf-budget` re-run, +2.25% then +2.30% raw, within budget. |
| iOS | **OTA number 39 on build 60** (2026-08-26) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Both of Android 71–72 in ONE publish, from `ignia-mac` after Android had been driven on a real device. Update group `8911ff63-e32e-47de-810f-af2cf6eacba0` (`01a0408e-37f5-74d9-98c3-cafb2c46a642`, from `dc689e20`). **Reaches the PUBLIC App Store** — build 60 is `READY_FOR_SALE`, so the blast radius is every iOS user rather than a tester list. Gate run ON THE MAC and parsed as JSON = `7b347b0f…` = build 60; export there; +2.30% raw / +2.41% gzipped, within budget. **Behaviour UNVERIFIED on iOS, and this one is layout**, which is the class that has differed across platforms here before — the tab strip is a new control and iOS safe areas differ. The identical JS is device-verified on Android in both themes. |
| Android | **vc 39 — BUILT, R8 ON, not yet released** (2026-08-26) | `ae526937893adb7f7349321b05caf2732da9658b` | **read from the `.aab`** | **The first Ignia binary with code shrinking, and the fingerprint did NOT move** — read out of the artifact as `ae526937…`, byte-identical to live vc 37, so every OTA already published reaches it and nobody is stranded. That is what made it safe to put R8 in this binary and hold Zero-Tap Sign-In (#101) for vc 40: `androidx.credentials` is a native dep and WOULD move it. `BUILD SUCCESSFUL in 6m 38s`; verifier green on all six checks including `READ_EXERCISE`. **R8 measurably ran:** AAB **66 MB → 59.7 MB**, `mapping.txt` carries **774,711** renamed entries, `usage.txt` is 6.5 MB of removed code — and the keeps held, with `expo.modules.ExpoModulesPackage` unobfuscated beside its synthetic lambda renamed to `expo.modules.a`. **The FIRST R8 run FAILED, and that is the good direction:** `Missing class com.google.firebase.ktx.Firebase` stopped the BUILD rather than letting a stripped class throw on a user's phone. R8 writes the rule it wants to `app/build/outputs/mapping/release/missing_rules.txt` — read that file, do not guess, and be suspicious of any entry that is one of OUR classes. **Where the config lives is load-bearing:** `enableMinifyInReleaseBuilds` is written by step 1c of `patch-android-release.mjs` into the gitignored `gradle.properties`, and the 117 lines of keeps live in the TRACKED `apps/mobile/android-proguard-rules.pro`, installed by step 1d — because `android/` is wholly gitignored and editing `proguard-rules.pro` in place loses it on the next `--clean` prebuild while the build carries on succeeding. **DEVICE-VERIFIED on the LG G6 (Android 9 / API 28), which is the only thing that could prove it** — a green build is not the test, because R8 removes what only reflection reaches and that throws at runtime rather than at compile time. Uninstalled the Play build, sideloaded the R8 APK, and exercised the whole reflection surface: cold launch with no crash, the sign-in screen rendering its SVG flame and custom fonts, **email/password sign-in through Firebase Auth**, the guided tour, Today painting real data (streak 36, 2,342 kcal left, maintenance 2,742), Trends with the fasting card, and **Sentry initialising with all sixteen native integrations**. No `ClassNotFoundException`, no `NoClassDefFoundError`, no `FATAL`. **One scare worth recording so the next person does not repeat the panic:** Today and Trends both sat on a spinner for roughly two minutes after first launch, which looks exactly like R8 having stripped the Firestore transport. It was a cold AsyncStorage cache on a 2017 phone — `ping` showed 0% loss to `firestore.googleapis.com` throughout, and the screen painted correctly on its own. **Wait several minutes on this device before diagnosing a data-layer failure.** The `NoClassDefFoundError: android/webkit/PacProcessor` in logcat is also a red herring: `PacProcessor` is API 29 and this device is API 28, it is logged at INFO by a WebView provider probe, and it predates R8. **Also note the verifier is AAB-shaped** — pointing it at an APK reports three bogus failures about `base/…` paths. |
| Android | **OTA numbers 66–68 on vc 37** (2026-08-26) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **The fasting card was rewritten by a device pass, and the first version was the lesson.** Live state is update group `b79742fb-1b6c-43d5-b080-f0930b487b07` (`01a04001-c2e7-79e6-80e1-6d3aaae6d16c`, from `63d9e535`); 66 and 67 were iterations published to LOOK at, which is what an OTA is cheap enough for. **The v1 card rendered perfectly and was a bad chart.** Twelve fasts of a real 16:8 habit (15h 30m–17h 30m) were seeded on the QA account and every bar landed between **65% and 73%** of a 24-hour strip — a barcode. There was no axis, so no height meant anything, and the median line was drawn in `faint` at the same height as the bars and vanished into them. **The root cause is structural and worth keeping: the sleep card's chart is DECORATION for its sentence — the highlighted bars ARE the claim. Fasting has no comparison it can honestly make, so its chart had to carry all the meaning, and a bar chart of near-identical values cannot.** Fixed by adding the sentence the chart could not say (a RANGE — pure description, no goal, no streak), drawing the median in `ink` ON TOP (you cannot compare twelve bar heights, but you can see whether their tops hug a line), and labelling the axis. **VERIFIED ON DEVICE, both themes and all four states** on the LG G6: the card in light and dark (the median inverts and stays visible), the 0-fast row, the mid-fast row — which must not tell someone sixteen hours in that they have no fasts — and the 2-of-3 row; the stub row's tap lands on Today **with the tab bar following**, confirming `replace` over `push` (a pushed tab route stacks a second Today over Trends). Gate `ae526937…` = vc 37 each time; export + `perf-budget` re-run (+2.25% raw, within budget). `WHATS_NEW_VERSION` not bumped. |
| iOS | **OTA number 37 on build 60** (2026-08-26) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | The iOS half of Android 66–68 — the rewritten card only, published once from `ignia-mac` after Android had shaken out the three iterations. Update group `934ec14f-a0f6-4909-b92e-7a19145230b7` (`01a04019-34d2-7262-9d25-cce9904c8b4e`, from `68b24399`). **Reaches the PUBLIC App Store.** Gate run ON THE MAC = `7b347b0f…` = build 60; export there; +2.22% raw, within budget. **Behaviour unverified on iOS**, as always — but unusually well covered by proxy this time: the identical JS is device-verified on Android in both themes and all four states. |
| Android | **OTA numbers 64–65 on vc 37** (2026-08-26) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **Fasting stops being destroyed, and then gets a card. Two publishes, deliberately separate.** **64** is `522af50a-eaff-4639-aaa5-02caf1e65ba2` (`01a03fd4-c814-71cb-9be7-c1179c0fe747`, from `85623dcd`) — **#97**, the data fix: `breakFast` used to null `fastStartedAt` and nothing else recorded the fast, so every completed fast was destroyed at the moment it became final. It is now a `writeBatch` that creates a `users/{uid}/fasts` interval and clears the timer in one commit, on BOTH frontends (the frozen web included — ADR-0022 freezes features, not correctness). **65** is `b8f26e9e-2d97-4ec5-8531-7d7bfac6cc48` (`01a03fe7-1944-70cb-9d17-3640c86fc224`, from `33dc38bc`) — **#98**, the Trends card. **#97 shipped alone and first on purpose**: it stops the bleeding, and the card cannot draw anything until fasts exist. **`firestore.rules` were DEPLOYED BEFORE either publish** — the dev app talks to PROD Firestore, so an un-deployed rule rejects the first write and reads like a broken feature. Gates read `ae526937…` = vc 37 before each; Metro export and `perf-budget` re-run each time (+2.07% then +2.16% raw, assets unchanged, within budget). `WHATS_NEW_VERSION` NOT bumped for either: #97 is invisible, and the card is unreachable for days (see below). **NOT device-verified, and the reason is not laziness — nobody can see the card yet.** It needs **three completed fasts inside a 14-day window** and before today nobody had a single one, because ending a fast deleted it. What every user sees first is the stub row. **The batch has a non-obvious failure mode that is worth knowing before touching `breakFast`:** a document the rules REFUSE fails the whole commit, so `fastStartedAt` would stay set and the user's timer would run forever with no way to stop it. The writer therefore validates the interval itself (`isStorableFast`, shared from `packages/core`) and drops a >14-day or inverted record rather than strand the timer. Six emulator cases against the production rules pin both directions. |
| iOS | **OTA numbers 35–36 on build 60** (2026-08-26) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | The iOS halves of Android 64–65 — same JS, published from `ignia-mac`. **35** is `8926ebc0-fa04-49ca-b529-065c275ad800` (`01a03fd5-b2d2-725f-a644-fcab233ca20a`, from `85623dcd`) and **36** is `84ad87ab-0766-4810-8878-10bdb53c48f1` (`01a03fe8-0dec-7798-b7c3-1c67f3d37b0b`, from `33dc38bc`). **Reaches the PUBLIC App Store** — build 60 is `READY_FOR_SALE`, so the blast radius is every iOS user. Gate run ON THE MAC and parsed as JSON both times = `7b347b0f…` = build 60; exports run there; +2.07% then +2.24% raw, assets unchanged, within budget. **A sequencing trap re-hit and caught:** the first export pair was taken BEFORE a last-minute fix (`33dc38bc`, the stub row switching tabs rather than stacking Today), so both were thrown away and re-run — an export is only as current as the commit it was built from, and publishing the stale one would have shipped the bug with a green gate. |
| Android | **OTA number 63 on vc 37** (2026-08-26) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **The Retake fix + multi-image capture, client halves.** Update group `e58283cc-6ba9-44ab-8689-17be35e041a0` (`01a03fb2-d695-7cd1-8451-dc14ae4a42db`, from `50fe7c54`). Two commits had been merged and reaching nobody: `9baa8453` (the Retake button was a squashed pill on the describe step) and `2ef17315` (multi-image capture, and the quota now counts images). **The server halves were already live** — `analyze-photo`, `daily-quota` and `spend-ceiling` deployed earlier the same day — so this is the JS catching up. Gate read `ae526937…` = vc 37 before publishing; Metro export run; bundle **+2.07% raw / +2.12% gzipped**, assets unchanged, within budget. `WHATS_NEW_VERSION` deliberately **NOT** bumped — it was set to `2026-08-26-tell-the-scan` hours earlier for this same feature and its copy already describes the note and the scale; re-firing nags whoever just dismissed it. **PUBLISHED WITH MULTI-IMAGE UNVERIFIED ON ANY DEVICE, and that was the owner's explicit call** when offered the alternative of holding it back: the photo pickers on both Android and the iOS simulator refuse automated selection, so no end-to-end scan has ever been driven through the app UI. Multi-image is unit-tested only, and its de-duplication is **improved, not solved** — the last synthetic run still produced both "baked sweet potato" and "mashed potatoes" off a crop and an 8° rotation of one illustration. **Judging it needs 2–3 real photos of one real meal.** The Retake fix is style-only and has never been seen either. |
| iOS | **OTA number 34 on build 60** (2026-08-26) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | The iOS half of Android 63 — same JS, published from `ignia-mac`. Update group `f0f1a379-9ba3-4086-b8fa-85e1f71a55ea` (`01a03fb4-0194-7e21-bb06-baed4826d836`, from `50fe7c54`). **Reaches the PUBLIC App Store** — build 60 is `READY_FOR_SALE`, so the blast radius is every iOS user, not a tester list. Gate run ON THE MAC and parsed as JSON = `7b347b0f…` exactly = build 60; export run there; bundle **+2.07% raw / +2.17% gzipped**, assets unchanged, within budget. Mac tree clean under `--untracked-files=no` — the `*` on the commit is the four untracked prebuild `Info.plist`/`Assets.xcassets` artifacts under `targets/`, which carry no JS. **Behaviour UNVERIFIED on iOS**, and unlike most rows that is not merely the usual no-iOS-device gap: multi-image is unverified on *every* platform. See the Android 63 row. |
| Android | **OTA number 62 on vc 37** (2026-08-26) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **#76 items 1–4 — the scan takes a note, reads a scale, and can skip the model entirely.** Update group `32a93941-cc4e-4cb3-96f9-414dca5483f2` (`01a03efa-d0b0-7e57-b768-bb81dd2d3786`, from `d7c76847`). Gate read `ae526937…` = vc 37 before publishing; Metro export run; bundle **+2.11% raw / +2.08% gzipped**, assets unchanged, within budget. `WHATS_NEW_VERSION` **bumped** to `2026-08-26-tell-the-scan` — the scan flow gains a visible step and an OTA has no store listing, so the banner is the only announcement it gets. **VERIFIED ON DEVICE against the published bundle** on the LG G6: `CheckCompleteUnavailable` after `DownloadComplete`, manifest id `01a03efa…` in the device log, the What's-new banner rendering the new copy, and the describe step laid out correctly at 360×720 dp — note field, help line, *Analyze photo*, *Retake*, nothing clipped. **The client half is inert without the server half**, which went out as `analyzephoto-00054`. **The device photo picker would not surface a pushed file** (present in MediaStore, absent from Recents), so a real scan was exercised through `scripts/smoke-photo-scan.mjs --note` against PROD instead — which is what found the defect below. |
| — | **What the note found, 2026-08-26** | — | `smoke-photo-scan.mjs --note` | **Running the new field against production immediately exposed a live resolver defect that predated it.** `cooked | chicken breast` resolved correctly to *Chicken breast, stewed, skin eaten*, but `cooked | skinless chicken breast` went to *Chicken, breast, boneless, skinless, **raw***. `stateBonus` is a RANKING signal (+30 cooked / −15 raw) and that raw row is the only one in the index carrying both `skinless` and `boneless`, so no bonus size closes the gap — **the same finding that forced the analogue guard on "Bacon strip, meatless"**. Cost: a 200 g portion read **224 kcal / 45 p** instead of **332 / 64.2**. Fixed by DISQUALIFYING a raw row on a cooked photo (`a99d11e0`), two-pass so it can never return `null` where it used to answer, with a COOKED marker winning outright over the ingredient-form words `dry`/`dried`/`mature seeds` — filtering on the raw marker alone sent `black beans` to *from canned, fat added*, caught by the existing case list. **Re-verified in PROD on `analyzephoto-00056`: 332 kcal / 64.2 p, resolved to *…skinless, boneless, meat only, cooked, braised*.** A first attempt blamed the model and added a prompt clause telling the note not to change `state`; that changed nothing, because the model was never the cause. **Deploy the prompt theory only after probing the resolver directly.** |
| iOS | **OTA number 32 on build 60** (2026-08-25) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | **Catches iOS up on THREE changes at once** — #83 (the splash gate), #85 (set-load bounds) and #77 Q5 (importer day boundary). Update group `89636535-b82a-4f5f-b883-91c967442aea` (`01a03bf0-e3a8-72ae-86fd-a89cbe0da499`, from `295d9435`). **Reaches the PUBLIC App Store** — build 60 is `READY_FOR_SALE`. Gate run ON THE MAC and parsed as JSON = `7b347b0f…` = build 60; export run there; bundle **+1.93% raw / +2.02% gzipped**, assets unchanged, within budget. Mac tree clean under `--untracked-files=no` — the `*` on the commit is the four untracked prebuild `Info.plist`/`Assets.xcassets` artifacts under `targets/`, which carry no JS. **The three sat Android-only for hours because `ignia-mac` went offline mid-session** (Tailscale showed it flapping, never SSH-able across four attempts); publishing from Windows was correctly refused by `guard_eas_update.py`, since the fingerprint follows the build host and a Windows publish would exit 0 and reach nobody. **Worth planning for: the Mac is a borrowed laptop and its availability is not under this project's control** — batch iOS publishes rather than assuming the host is there. **Behaviour UNVERIFIED on iOS** (no iOS device here); #83 is device-verified on Android in detail, and #85/#77 are inert on the default boundary and on in-range loads. |
| Android | **OTA number 61 on vc 37** (2026-08-25) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **#85 (set-load bounds) and #77 Q5 (importer day boundary) — ADR-0030 is now ACCEPTED and the ratchet is at 0.** Update group `7096fda6-1be9-4162-ac8b-8b43fea6312e` (`01a03be1-b579-7ab0-9f31-7b84e0a17224`, from `21cec7f7`). **#85's rule is that the bound follows the MOVEMENT**: a single ceiling cannot separate a 1,275 lb glute bridge from a 1,275 lb leg press, so `weight-reps` gets 1500 lb and `bodyweight` gets 200 lb — built with one global ceiling first and it did NOT catch the reported defect. **#77 Q5's rule is that we derive the day only when the source did not**: raw samples converted, OS-bucketed daily totals keep the source's day (the OS bucketed 00:00–24:00 at calendar midnight), sleep keeps ADR-0033's wake-day rule. The two cardio merge keys moved TOGETHER — they are compared to each other, so converting one would stop a 00:30 run finding its own session. **Inert for every existing account**: all are on MIDNIGHT, under which `dayKeyAt` IS the calendar date, and set loads in range are untouched. `WHATS_NEW_VERSION` not bumped — neither is user-visible. Gate, export and `perf-budget` re-run; +1.99% raw, within budget. **NOT device-verified** and deliberately: both changes are inert on the default boundary and on in-range loads, so there is no observable behaviour to capture without first seeding a 3 AM account. **iOS NOT published — `ignia-mac` is offline** (Tailscale: last seen minutes ago, flapping); `guard_eas_update.py` correctly refuses an iOS publish from Windows. |
| Android | **OTA numbers 58–60 on vc 37** (2026-08-25) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **#83 — the residual splash hang. Live state is update group `a1c82c35-cba5-4817-8734-af2937e45418`** (`01a03b37-677e-71c7-9b4d-e6ffb585a4ba`); **58** `b0a7540f…` was TEMPORARY INSTRUMENTATION and **59** `5d145f5d…` was a first cut that did not work. **The instrumentation is the point of this row.** #79 left a 10–15 s hang and the cause was inferred, not measured. Shipping a logging-only OTA to Android alpha and reading logcat under the Private-DNS black-hole settled it: `onAuthStateChanged` fires at **771 ms** on a working network and at **8,690 ms** when DNS is dead, because Firebase validates the restored token before it will emit. `GATE_MAX_WAIT_MS` fired at **16,734 ms** and contributed nothing — the two waits are sequential, as suspected. **An OTA is cheap enough to spend on a measurement; do that before designing a fix.** The fix reads Firebase's own persisted session out of AsyncStorage after a 1200 ms grace, so the presumed path only ever runs when the network is genuinely failing. **59 shipped and did NOT work, and only the device showed it**: the splash lifted off `initializing` and was instantly re-raised, because with a presumed session the profile had never started loading and `assessRoute` returned 'wait'. 449 green specs saw none of it. **VERIFIED ON DEVICE, both paths.** Black-holed: interactive at **~6 s** (avatar `?`, presumed) and the real session lands by **12 s** (avatar `QT`) with no splash flap-back — the #79 latch held. Good network: interactive at 6 s showing `QT` throughout, so the presumed path never engaged, full data by 9 s. Before #79 this was >30 s. Gate, export and `perf-budget` re-run; +1.98% raw, within budget. Published from the branch at the v2 commit; the squash on `main` (`b120371a`) is content-identical. |
| Android | **OTA number 57 on vc 37** (2026-08-25) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **Trends day boundary (PR #84).** Update group `b3c9525d-b9be-4ad5-ba5e-75d50235ebc3` (`01a03b19-812b-7cab-abd7-1f7f4648094e`, from `f4f28663`). `insights`, `loggedThisWeek`, `weightSeries` and `budget` were midnight-only while the sleep card was boundary-aware, so one screen keyed two cards to two calendars. **The ratchet does not catch this class** — `check:day-boundary` greps for `calendarDateKey` and none of these call it; it still reports 10 before and after. `WHATS_NEW_VERSION` not bumped (invisible on the default MIDNIGHT boundary, which is every account today). Gate, export and `perf-budget` re-run; +1.95% raw, within budget. |
| iOS | **OTA number 31 on build 60** (2026-08-25) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | The iOS half of Android 57 — same JS, published from `ignia-mac`. Update group `e2fff799-585a-4d81-9278-0f25407d63d7` (`01a03b1a-7f55-799f-994a-f4a8f84da1ec`, from `f4f28663`). **Reaches the PUBLIC App Store.** Gate run ON THE MAC = `7b347b0f…` = build 60; export run there; +1.89% raw / +1.96% gzipped, within budget. |
| iOS | **OTA number 30 on build 60** (2026-08-25) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | The iOS half of Android 56 — #79, same JS, published from `ignia-mac`. Update group `cc3f50be-e006-4d7c-b097-a190da6d2972` (`01a03ace-889f-7b52-a8e8-fbf4ec6cec45`, from `17ffb169`). **Reaches the PUBLIC App Store** — build 60 is `READY_FOR_SALE`. Gate run ON THE MAC and parsed as JSON = `7b347b0f…` exactly = build 60; export run there; bundle **+1.89% raw / +1.97% gzipped**, assets unchanged, within budget. Mac tree clean under `--untracked-files=no` — the `*` eas prints on the commit is the four untracked prebuild `Info.plist`/`Assets.xcassets` artifacts under `targets/`, which are generated and carry no JS. **Behaviour UNVERIFIED on iOS** — no iOS device here. The identical JS is device-verified on Android against a deterministic bad network (see the Android 56 row), and this change is pure gate/state logic with no layout, which is the class least likely to differ across platforms. **The residual `initializing` hang ships with it and is not fixed on either platform.** |
| Android | **OTA number 56 on vc 37** (2026-08-25) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **#79 — the splash overlay hang + latch.** Update group `4b96d2c4-e1a4-4028-b9e7-ef828860504c` (`01a03abe-c78e-7755-8479-8df769dc9185`, from `e80419e7`). `WHATS_NEW_VERSION` deliberately NOT bumped — this is a bug fix, and the banner fired hours earlier on `2026-08-25-sleep`. Gate, Metro export and `perf-budget` re-run before publishing; bundle +1.95% raw, within budget. **VERIFIED ON DEVICE, and the verification needed a deterministic bad network to be worth anything.** The LG G6's own wifi black-holes at random, which is unreproducible; `settings put global private_dns_mode hostname` + a bogus `private_dns_specifier` reproduces it exactly and reversibly — wifi stays `CONNECTED/CONNECTED` while DNS and `ping 8.8.8.8` both die. **Before: splash still up at 30 s, gone by 45 s. After, same condition, twice: up at 10 s and gone by 15 s on a cold cache; fully rendered with real data by 9 s on a warm one.** **NOT verified on a good network** — the AP stopped routing entirely and wifi bounces no longer recovered it, so a fast-path timing was never captured. **A residual hang remains and is NOT fixed**: `initializing` still waits on Firebase Auth's own `onAuthStateChanged`, which does not emit until its token refresh gives up (~5–7 s here), and `GATE_MAX_WAIT_MS` then runs *after* that rather than across it. |
| Android | **OTA number 55 on vc 37** (2026-08-25) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `f9efb1e3-9047-4da9-bf8f-15b2e3bc0085` (`01a03a56-171b-7b6b-a625-fc682e5edf8e`, from `91ac53c`). **`WHATS_NEW_VERSION` bumped to `2026-08-25-sleep`** — the sleep card shipped in OTA 53 with the banner deliberately NOT bumped, on the reasoning that `2026-08-25-day-start` had fired hours earlier and re-firing nags whoever just dismissed it. **The owner overruled that and was right**: an OTA has no store listing, so the banner is the only announcement a feature gets, and sleep on Trends is the most visible thing to land in weeks. **VERIFIED ON DEVICE**: two restarts (`DownloadComplete` → `CheckCompleteUnavailable`), banner renders in full on Today above the hero with the feedback link under it. Gate, Metro export and `perf-budget` all re-run before publishing; bundle +1.94% raw, within budget. |
| iOS | **OTA number 29 on build 60** (2026-08-25) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `36fdbfab-2175-4110-bad3-dd1d57020ec3` (`01a03a58-74da-7dc2-bfe5-d2dab03bf391`, from `91ac53c`), published from `ignia-mac`. The iOS half of Android 55 — the What's-new banner. **Reaches the PUBLIC App Store.** Gate parsed as JSON on the Mac = `7b347b0f…` = build 60; export run there; bundle +1.88% raw, within budget. |
| Android | **OTA numbers 53–54 on vc 37** (2026-08-25) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **#80 (the sleep import guard) and #81 (sleep on Trends).** Final state is update group `1148fc2e-0ccb-493c-b434-8b6d0c57b2c6` (`01a03a42-5ed3-750d-88e3-818a2ca130ac`, from `eb8df63`); **53** is `e3678282-3b35-499a-af61-ff6f94aaeff4` (`01a03a31-acf7-7ab8-b893-46f0defb6b34`, from `cf260f07`) and **54** carries the empty row's tap-through. **The headline is that this went out on vc 37 at all.** Yesterday's handoff said the Android channel was SHUT because vc 38 moved the fingerprint to `b0849308…` — true when written, and no longer true: `cf260f07` rehomed `READ_EXERCISE` out of the hashed `app.json`, which returned the Android tree to `ae526937…` **= vc 37, already live on alpha**. So no binary was needed. Gate read from the tree before each publish; Metro export run; bundle **+1.94% raw / +1.90% gzipped**, assets unchanged, within budget. `WHATS_NEW_VERSION` deliberately NOT bumped — sleep on Trends is user-visible, but the banner already fired today on `2026-08-25-day-start` and re-firing nags anyone who just dismissed it; worth bumping on the next publish. **VERIFIED ON DEVICE against the published bundle** on the LG G6 (`CheckCompleteUnavailable` + `NoUpdatesAvailable` on the launch after `DownloadComplete`), all THREE ADR-0033 states screenshotted at 360×720 dp on a seeded QA account: the full card (`6h 45m a night, last 14`, 7 blue short bars against 7 grey, median line sitting between the two groups, *"On your 7 shortest nights you logged 2,518 kcal — 567 more than on your 7 longest"*, footer `14 of 14 nights · imported`), the 5-night progress state (gaps drawn as baseline hairlines, *"At 12, Ignia will line your shorter nights up"*), and the 0-night row (no card, no section header, hairline-bounded). **The device pass found a real defect the 427 green specs did not**: the empty row's chevron was inert. That is OTA 54. |
| iOS | **OTA numbers 27–28 on build 60** (2026-08-25) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | The iOS half of Android 53–54 — same JS, published from `ignia-mac`. **27** is update group `91768d6a-a40d-4add-bc0a-817d1bbe6bde` (`01a03a32-5002-7b77-8f80-5afa178ded8e`, from `cf260f07`) and **28** is `ab1e1cae-2f20-43e5-8f63-09ee0edf57e9` (`01a03a42-a5af-7a91-bbd6-4577f7a0d982`, from `eb8df63`). **Reaches the PUBLIC App Store** — build 60 is `READY_FOR_SALE`. **And it only reaches anyone because of `cf260f07`:** `dd3ceb10` had moved the iOS runtime to `f319b32d…` earlier the same day, off build 60, so the iOS channel had been silently shut for hours and the handoff wrongly recorded it as open. Gate run ON THE MAC and parsed as JSON both times = `7b347b0f…` exactly; export run there; bundle **+1.87% raw / +1.95% gzipped**, assets unchanged, within budget; Mac tree clean under `--untracked-files=no`. **Behaviour UNVERIFIED on iOS** — no iOS device here — but the identical JS is device-verified on Android in detail above, and this card is layout-heavy, which is the class that has differed across platforms before. |
| Android | ~~**vc 38**~~ **ORPHANED 2026-08-25** | `b08493087dc6d304c07e3f0499e39cabcd601e78` | **read from the `.aab`** | **Built, verified, never released — and now unreachable.** `cf260f07` rehomed `READ_EXERCISE` out of the hashed `app.json` (see the `app.json` section below), which returned the Android tree to `ae526937…` = **vc 37**. This artifact embeds `b0849308…`, so no OTA will ever reach it. Nothing is lost: it was never on the track. **Its submit failed and exited 0 — a FIFTH way that command lies.** `eas submit` reported success while fastlane's log carried *"Google Api Error: Invalid request - You must let us know whether your app includes any health features"* five times; the androidpublisher tracks API still read vc 37, which is how it was caught. **The cause is a real deadlock and it reverses what `CLAUDE.local.md` recorded:** Play refuses to RELEASE a bundle whose health permissions the Health apps declaration does not cover, and the declaration's step 1 lists permissions **as detected in the bundle**, so `READ_EXERCISE` cannot be declared until it ships and cannot ship until it is declared. Uploading as a **DRAFT** release breaks half of it — committed cleanly via androidpublisher (`edits.insert` → `bundles.upload` → `tracks.update` with `status: 'draft'` → `commit`), proving the check fires on review and not on upload — but the declaration still did **not** list the permission afterwards, so a draft is not enough. **CORRECTED 2026-08-26: that draft is NOT on the alpha track.** The tracks API reads `alpha` = vc 37 `completed`, and the only draft anywhere is an EMPTY one on `internal` with no versionCodes; vc 38 is an uploaded bundle attached to no track. Nothing to discard. **RESOLVED 2026-08-27 — see the vc 39 row above; the route is a Console release to INTERNAL testing, which makes Play detect the permission.** The declaration is at Policy and programs → App content → Actioned → Health apps, and its step 1 *App features* boxes were already correct. Two API notes: `edits` answers a transient **503 `backendError`** under load (the 67.5 MB upload succeeded and the very next `tracks.update` got one — retry the small calls, never blind-retry the upload), and the `.aab`'s sha256 is `4b06df5d92797effbd665d889add2157547f1efcefecf91a9456fcf7877f342a`. |
| iOS | **OTA number 26 on build 60** (2026-08-25) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `565d6f3d-c809-44ff-bd32-3e4d7d7d30db` (`01a03947-5be3-72e8-ab5c-ce0172fa3014`), from `b616f23c`, published from `ignia-mac`. The iOS half of Android OTA 52 — ADR-0030 (#77). Gate run **on the Mac** and read `7b347b0f...` = build 60; export run there; bundle **+1.59% raw / +1.69% gzipped**, assets unchanged, within budget. **This reaches the PUBLIC App Store, not just TestFlight** — 1.2.1 / build 60 is `READY_FOR_SALE`, so build 60's runtime is what public iOS runs, and the blast radius is every iOS user rather than the tester list. **iOS is NOT device-verified** — no iOS test device exists here, which is the standing shape of this project and not a new gap. **Mac disk was freed first**: `DerivedData` alone was 10.3 GiB and the volume went 7.7 GiB → **19 GiB** free. The '~600 MiB free' figure this repo has been carrying since 2026-08-22 was **stale** — it read 7.7 GiB on connect, before any cleanup. |
| Android | **OTA number 52 on vc 37** (2026-08-25) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `3af2e5d8-6af6-4b78-a3c6-7f1411675b8e` (`01a03942-d151-7b46-8597-8a4dd76ee86c`), from `77068da8`. **ADR-0030 complete (#77): the day boundary is a setting.** Settings → *Day starts at* (midnight · 3 AM · 6 AM), three locales; `dayBoundary` on the profile with `firestore.rules` **deployed BEFORE** the publish, which is the ordering that matters — the dev app talks to PROD Firestore, so an un-deployed rule rejects the first write. Gate read `ae526937...` = vc 37 before publishing; Metro export run; bundle **+1.73% raw / +1.63% gzipped**, assets unchanged, within budget. `WHATS_NEW_VERSION` **bumped** to `2026-08-25-day-start` — unlike the last several publishes this IS a user-visible feature, and an OTA has no store listing to announce it. **Everything defaults to `MIDNIGHT`, under which `dayKeyAt` is byte-for-byte the calendar date**, so no existing account's numbers move until someone opens the row: core 1,180 · mobile 407 · web 206 · functions/rules 515 all green, and the 1,160 core tests that predate the change passed unmodified. **NOT yet device-verified** — see the row's own caveat below. |
| Android | **OTA numbers 44–51 on vc 37** (2026-08-24/25) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **Live state is update group `de4df19e-5472-4b23-acff-13210e0f0ea7` (50), republished as `257813bb-1419-46c2-a77a-0f9eeb8ec4af` (51) for a device capture.** **44** is `4a76f432-33d6-416d-84db-0bba01575acc` — the Settings 17→13 refactor, published by the previous session and never given a row here. **45** `f2f26336-c4d7-43bf-acce-561c4b4962be` and **50** `de4df19e…` are ADR-0031 (#78): `reloadScreenOptions` on both reload paths plus banner copy that says how long, in all three locales. **46–49 are verification republishes of identical JS, not changes** — the reload screen can only be observed on the restart AFTER the bundle carrying it is running, so seeing it at all costs one extra publish per attempt. **The headline is a NEGATIVE result and it is measured, not suspected: `reloadScreenOptions` does NOTHING on Android.** The same restart was captured with two different `backgroundColor` values shipped over the air — `colors.paper` and then `colors.heroPanel` (`#161412`) — and the gap was the identical flat `#faf9f6` both times, with **zero** coral pixels anywhere in a 480×480 centre crop across every frame. A 72dp `large` spinner tinted `#ff6a3d` is ~250px on this screen; nothing of ours is drawn. Cause is upstream and is in the installed source: `RelaunchProcedure` calls `reloadScreenManager?.show(weakActivity?.get())`, `show()` returns early on a null activity, and `weakActivity` is captured once at React-context init from `reactContext.currentActivity` — null during a cold start, which every restart we can produce belongs to. **There is no JS workaround**: `Updates.showReloadScreen()` exists but its native body is wrapped in `if (BuildConfig.DEBUG)`. **What the capture DID buy is the number ADR-0031 was blocked on.** Screen-recorded at 10 fps with `dev.expo.updates` logcat as the clock, twice: **restart → Today painted is ~4.6–5.0 s on the LG G6, and ~80% of it (~3.5–4.2 s) is APP BOOT**, not the pre-JS gap (~0.7–1.0 s) — the exact condition the ADR named for calling the reload screen cosmetic. **And the banner could not be made to appear at all** across ~8 foreground cycles with an update pending: `useAutoApplyOta` won every time, and `UpdateBanner` is additionally suppressed behind What's new by `useTodayNudge`'s ranking. That withdraws option C, which existed to make a rare banner rarer. **Do not report the Android restart as fixed.** `WHATS_NEW_VERSION` deliberately NOT bumped — nothing here is a user-visible feature and the banner already fired today. |
| iOS | **OTA number 25 on build 60** (2026-08-24) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `f99c81aa-efae-41f9-b4ee-41c2689b68ba` (`01a03619-3281-79b7-ab53-5a44fe5586e7`, from `7be5a9fd`), from `ignia-mac`. **Automatic Oura import, and Apple Health moved into Connected apps.** Reaches the public App Store. **The first attempt FAILED and it was self-inflicted — worth recording.** A polling command was written as `ssh ... 'grep ...; rm -rf apps/mobile/dist'`, and when the poll loop timed out the `rm` ran **while the upload was still in flight**, deleting the bundle out from under it: `Failed to upload / ENOENT ... index-*.hbc`. **Never put a `dist` cleanup in the same command as a publish check.** Clean up only after `Published!` is on screen; the re-run then pays a cold Metro cache for nothing. |
| Android | **OTA number 43 on vc 37** (2026-08-24) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `d145a90d-1a6b-4418-91d1-884d3cb1864a`, from `7be5a9fd`. The Android half of iOS OTA 25. Gate = vc 37; export run; bundle +1.64% raw, within budget. **VERIFIED ON DEVICE**: `CheckCompleteUnavailable` on the LG G6 after the usual retry through `CheckError` — this radio has now needed 2–3 launches on every OTA today and it is not a publish signal. |
| iOS | **OTA number 24 on build 60** (2026-08-24) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `03202723-94c1-4f3f-b37a-6ce7716b3224` (`01a035f5-6ecf-72c2-a5d0-216458a69107`, from `5aab5b6b`), published from `ignia-mac`. **Oura `daily` scope (sleep + activity), the self-closing auth modal, and the connect success state.** Reaches the **public** App Store. **Deploy order was rules -> functions -> OTAs and all three were required:** `dailySleep` gained a rules-enforced `source` field (CLAUDE.md says deploy rules BEFORE a client writes a new top-level field), and `fetchOuraDaily` is a NEW callable — probed after create and it answers **401**, not the 403 that would mean the `run.invoker` trap from README's operator checklist had bitten. **Every already-connected Oura user must reconnect**, which is what the scope-upgrade notice shipped in OTA 22/23 exists to tell them. **Behaviour UNVERIFIED on iOS** — and the modal fix is iOS-specific by nature (`ASWebAuthenticationSession` cannot intercept an `https://` redirect without Universal Links), so this is the one to watch on a real iPhone. |
| Android | **OTA number 42 on vc 37** (2026-08-24) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `1c7206cc-dca3-4b56-8867-15874729ba07`, from `5aab5b6b`. The Android half of iOS OTA 24. Gate read `ae526937...` = vc 37; Metro export run; bundle +1.63% raw, within budget. **VERIFIED ON DEVICE**: `CheckCompleteUnavailable` on the third launch of the LG G6. **A publish-reporting trap re-hit, and worth remembering:** the detached log stopped mid-`Uploading` with no `Published!` line and the process was gone, which reads exactly like a silent death — but `npx eas update:list --branch production` showed the group published *just now*. **The log is not the authority; `eas update:list` is.** Do not re-publish on the strength of a truncated log. |
| iOS | **OTA number 23 on build 60** (2026-08-24) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `373cfc13-e744-4bd0-a9ca-c362b35ced8b` (`01a035c0-33c3-7ea8-bc86-350d49f0b67a`, from `fcf1bdcb`), published from `ignia-mac`. **Connected apps screen + the Oura scope-upgrade check.** Reaches the **public** App Store (build 60 is live). Gate parsed as JSON on the Mac = `7b347b0f...` = build 60 exactly; Mac tree clean under `--untracked-files=no`, so the published JS is exactly `fcf1bdcb`. **Pairs with a functions deploy that must land FIRST** - `fetchOuraWorkouts` now stamps `lastSyncedAt`/`lastRecordCount` on `users/{uid}/integrations/oura`, and without it the new screen's evidence row renders "Not checked yet" forever. A client cannot write those fields: `integrations/{provider}` is `allow write: if false`. **Behaviour UNVERIFIED on iOS**; the Android half is device-verified below, and this is a layout-heavy change, which is the class that has differed across platforms here before. |
| Android | **OTA number 41 on vc 37** (2026-08-24) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `795d710f-ec54-45aa-81f4-5dbbd33f3050` (`01a035bf-f30a-736a-aa37-a02642fb053b`, from `fcf1bdcb`). The Android half of iOS OTA 23. Gate read `ae526937...` = vc 37 before publishing; Metro export run; bundle +1.59% raw, within budget. **VERIFIED ON DEVICE against the published bundle** on the LG G6: `CheckCompleteUnavailable` on the third launch, and the new screen was then opened by deep link (`adb shell am start -a android.intent.action.VIEW -d "ignia://connected-apps"` - faster and far more reliable than driving Settings by taps) and screenshotted. Header, provider card, status pill, Connect button, the collapsed *What Ignia reads* disclosure and the footnote all render, **nothing clipped** - which is the point, since this OTA also carries the fix for the `Import now` button that rendered as "Impo" off the right edge. Launch 1 hit `CheckError` and launch 2 `DownloadComplete`: the same flaky-radio pattern as OTA 40, not a bad publish. `WHATS_NEW_VERSION` deliberately NOT bumped - third publish of the day and the banner already fired on `2026-08-24-cardio`. |
| iOS | **OTA number 22 on build 60** (2026-08-24) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `001638a0-1cdf-4a46-8d66-f27bd62d4ea6` (`01a0358f-d368-7e32-890d-0ea29b669469`, from `eb921e2c`), published from `ignia-mac`. **Restaurant food from 91 chains** (ADR-0027, #67). Gate parsed as JSON on the Mac = `7b347b0f…` = build 60 exactly. **This one reaches the PUBLIC App Store**, since 1.2.1 / build 60 released that morning. **The corpus itself is NOT in this bundle and deliberately never will be** — it is 4.3 MB against an on-device index that is already 1.4 MB. What ships is the 91 chain NAMES (~2 KB, `packages/core/src/restaurant-chains.data.ts`, generated by the ingest) used as a router: a query naming a chain calls `searchFoods`, everything else stays on-device and offline. **So this OTA is inert without the functions deploy** that carries `restaurant-foods.json` — deploy functions FIRST or the client silently falls back to local results, which looks exactly like the feature not working. Mac tree clean under `--untracked-files=no`, so the published JS is exactly `eb921e2c`. **A polling trap worth recording:** `pgrep -fc eas-cli` returns 0 for a *running* publish — the process is `node …/eas`, not `eas-cli`, so a check on that pattern reports a live publish as dead. Match on `eas update` instead. **Behaviour UNVERIFIED on iOS**; the Android half is device-verified below. |
| Android | **OTA number 40 on vc 37** (2026-08-24) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `14ee0787-460e-413f-98ed-07f92474d5c9` (`01a0358e-0eef-7a9f-8805-4fdaee22cf18`, from `eb921e2c`). The Android half of iOS OTA 22 — same JS, the chain-name router. Gate read `ae526937…` = vc 37 before publishing; Metro export run; bundle within budget (+1.42% raw, +1.35% gzipped). **VERIFIED ON DEVICE against the published bundle** on the LG G6: `CheckCompleteUnavailable` on the final launch, i.e. it is running this update with nothing newer to fetch. **It took THREE launches, and the failure mode is worth knowing before anyone calls an OTA broken on this device:** launches 1 and 2 both ended `DownloadError` at `downloadProgress: 0.967…` — **30 of 31 assets**, one asset short — and launch 3 reached `DownloadComplete`. Nothing was wrong with the publish; this is a transient asset fetch on a 2017 phone, and `expo-updates` resumes rather than restarting. `WHATS_NEW_VERSION` deliberately **NOT** bumped: the banner was set to `2026-08-24-cardio` earlier the same day and re-firing nags anyone who just dismissed it — **worth bumping on the next publish, because unlike the Oura surface this one is visible to every user.** |
| iOS | **OTA number 21 on build 60** (2026-08-24) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `31528192-3395-4103-a713-e7cf4263117a` (`01a0353e-596f-70e7-b8bc-0fa2b664ef71`, from `e110391e`), published from `ignia-mac`. The iOS half of Android OTA 39 — same JS: the **Oura Cloud connect surface** (#72) and the **cardio glossary** (#66). Gate parsed as JSON on the Mac = `7b347b0f…` = build 60 exactly; `expo export --platform ios` run first; tree clean under `--untracked-files=no` both before and after, so the published JS is exactly `e110391e` (the `*` on the commit line is the untracked `dist/` the export wrote). Mac disk 12 GiB free. **A NEW PUBLISH TRAP, and it hit BOTH platforms at once:** `eas update --non-interactive` now **requires `--environment`**, and without it the command fails instantly with *"The `--environment` flag must be set"* — publishing nothing. Run detached as the OTA-38 row advises, that failure is silent: the process exits, the log holds the error, and nothing in the session says so. **Read the log for `Published!` before believing a detached publish landed** — the first attempt on both platforms failed exactly this way and was only caught by reading the log. **Reaches TestFlight build 60 only**; public iOS is build 55, a different runtime. **Behaviour UNVERIFIED on iOS**, and this one carries the caveat that matters most so far: the Oura path has never been exercised against the one real ring on this project, on either platform. |
| Android | **OTA number 39 on vc 37** (2026-08-24) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `10cadd56-6d38-4f6c-b5c2-28e2c64091ee` (`01a0353e-c80d-7011-ade9-55055c1e1c8c`, from `e110391e`). The **Oura Cloud connect surface** (#72) plus the **cardio glossary** (#66). Gate read `ae526937…` = vc 37 before publishing; Metro export run; bundle within budget (+1.35% raw, +1.29% gzipped). **This OTA is worth more on Android than on iOS, and the reason is a permission it does NOT need.** The health-store workout read requires `READ_EXERCISE`, which moves the Android fingerprint and therefore waits on vc 38. The Cloud API path touches no health permission at all — so `expo-web-browser` was already linked by `auth.tsx`, the fingerprint did not move, and **Oura import reached Android testers a whole binary earlier than the health path can.** `WHATS_NEW_VERSION` deliberately **NOT** bumped: the banner was set to `2026-08-24-cardio` hours earlier by OTA 38 and re-firing nags anyone who just dismissed it — and announcing an Oura integration to 12 testers, approximately none of whom own a ring, would be noise for an integration no real ring has yet exercised. **Not device-verified.** |
| iOS | **OTA number 20 on build 60** (2026-08-24) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `3800406a-b413-4b0d-873c-c4131cd36efa` (`01a033aa-fcd8-7abb-a105-3f69da8eca1d`, from `b85553c7`), published from `ignia-mac`. **The first OTA in a long while that reaches the PUBLIC App Store**, because 1.2.1 / build 60 released that morning — every earlier OTA against `7b347b0f…` was TestFlight-only. Carries the **formula→measured crossing ramp**: `measuredConfidence` gained a third ratio on evidence quantity, so confidence climbs 0→1 across logged days 14→28 and the mode boundary stops being a **−449 kcal step** (now **+3**; largest single-day move anywhere 449 → 96, convergence unchanged). Gate parsed as JSON on the Mac = `7b347b0f…`; `expo export --platform ios` run first; Mac tree clean under `--untracked-files=no`. **Disk needed clearing first — the Air was at 798 MiB, 100% full**; DerivedData, simulator caches and stale sims took it to 3.9 GiB. Behaviour unverified on iOS hardware as always; the identical JS is device-verified on Android below. |
| Android | **OTA number 38 on vc 37** (2026-08-24) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `7ba5a642-da02-431a-9831-8fb864bf31d9` (`01a033a9-0695-7e8a-a396-1a5417c0cc2b`, from `b85553c7`). The Android half of iOS OTA 20 — same JS, the crossing ramp. Gate read `ae526937…` = vc 37 before publishing; Metro export run; bundle within budget (+0.71%). **VERIFIED ON DEVICE against the published bundle**: the LG G6 downloaded `01a033a9…`, reported `NoUpdatesAvailable` on the next launch, and Today renders correctly. **And the ramp surfaces its own explanation** — the account is now `provisional` (`tdee.confidence < 1`), so `HeroRings` shows the line that already existed for exactly this state: *"Your target is held steady while the record fills in"*, translated in all three locales. The damping is not silent, and that came for free because the app already modelled "we did not act on this at full strength". **A publish trap re-hit:** `eas update` run in the foreground was killed at the 2-minute tool limit and published NOTHING, while looking like a failure that might have half-landed — `eas update:list` is the only way to tell. Run it detached. |
| iOS | **OTA number 19 on build 60** (2026-08-23) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `79a0315b-5275-42b4-aa41-a81f5a9cd606` (`01a0316f-10aa-78fb-b1b4-e3f125555611`, from `0eb0ef13`), published from `ignia-mac`. **One line of app code, and it is a QA affordance, not a feature**: `Glossary` now passes `backdropTestID="glossary-backdrop"` to `<BottomSheet>` — the affordance `EntrySheet` has always had. Nothing a user can see changes. It ships because the alternative is a COORDINATE dismissal, and that is viewport-dependent: on the LG G6 (360x720dp) the Train glossary — the longest of the three — reaches to ~8% of the screen height, so `19-glossary`'s `50%,8%` backdrop tap landed on the sheet, the sheet stayed open over the tab bar, and the run died two steps later at *Tap on Today*, which reads like a broken tab bar. **Android was published the same day** (update group `6a58b25d-2faa-47c8-afdb-a0f4d495334b`, `01a0318b-3cab-7ab4-8abd-0072d3f7aa21`, runtime `ae526937…` = vc 37, from the Windows workstation as the guard requires), and the LG G6 was confirmed running it. **The id then turned out NOT to be usable for the dismissal, and that is worth more than the fix would have been**: the backdrop is `<Pressable style={StyleSheet.absoluteFill}>`, so its bounds are the whole screen and Maestro taps an element's CENTRE — which on a content-height sheet is behind the panel. `tapOn: id: 'glossary-backdrop'` reports COMPLETED and dismisses nothing. The suite stays on `50%,5%`; the prop is inert and harmless, and `19-glossary` carries the disproof so nobody re-derives it. Gate parsed as JSON on the Mac and read `7b347b0f…` = build 60 exactly; `expo export --platform ios` run before publishing; `git push` done first with `origin/main..HEAD` at 0; the Mac's tree was clean under `--untracked-files=no` both before and after, so the published JS is exactly `0eb0ef13`. **Reaches TestFlight build 60 only** — public iOS is build 55, a different runtime. **Behaviour UNVERIFIED on iOS** and deliberately so: there is nothing to verify, since the prop is inert at runtime. |
| iOS | **OTA number 18 on build 60** (2026-08-23) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `aaf73120-1a22-49a1-8e27-060cd8788c0f` (`01a03003-bc14-7313-a3c9-3af6bbad2d49`, from `f9e071be`), published from `ignia-mac`. Two unhandled promise rejections found by Sentry triage, not by a user report. **The one that mattered was on the PUBLIC build**: `IGNIA-MOBILE-8`, ten events from one iPhone 17,2 on release 1.2.0 **build 55**, every one `Unable to open URL: https://ignia.fit/support` — five bare `Linking.openURL` calls in Settings with no catch, so a tap on *Help and support* did nothing at all, silently, ten times across ten days. `openExternal()` now alerts with the URL. The second, `IGNIA-MOBILE-E`, is `useHealthAutoImport` writing `void importHealth(uid)` — `void` discards the VALUE and leaves the rejection unhandled, so HealthKit refusing a read on a locked device (`Code=6`, documented Apple behaviour) was reported like a crash. **This OTA reaches TestFlight build 60 only, which is NOT where the reporting user is** — build 55 is public iOS on a different runtime, so that user keeps the dead link until the next App Store release. |
| Android | **OTA number 36 on vc 37** (2026-08-23) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `5945994f-3968-4c19-8273-8a8897968179` (`01a03002-b792-7212-aadc-08a3e079a616`, from `f9e071be`). The Android half of iOS OTA 18 above — same JS. **Published from WINDOWS, and the guard is why.** `guard_eas_update.py` refused the Mac: the runtime fingerprint follows the build host, Android binaries are built here, and a Mac publish would have landed on a runtime no shipped Android binary carries — exit 0, reaching nobody. Gate read `ae526937…` = vc 37 on this workstation before publishing. |
| iOS | **OTA number 17 on build 60** (2026-08-23) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `0426e031-512d-443e-bd54-852949e3f074` (`01a02fb6-bfaf-7897-9e8a-dec612e69489`, from `72035b47`), published from `ignia-mac`; gate `7b347b0f…` = build 60, unchanged, so no binary. **One iOS publish carries everything Android took three for** (33–35 below) — same JS, same tip, the Mac simply published once at the end instead of per fix. Reaches **TestFlight build 60 only**; public iOS is build 55, a different runtime, so this is not shipped to App Store users. **Not device-verified on iOS** (no iOS device here); the Android half is device-verified on the LG G6. |
| Android | **OTA numbers 33–35 on vc 37** (2026-08-23) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Three publishes, one per fix, all confirmed running on the LG G6. **33** `99c75b5e-4946-4f9f-bf93-a5c1a63b278b` (`01a02f84-f363-7d41-85a0-0306b42f1cd1`, from `78e8ede3`) — Portuguese (Brazil) throughout, plus the locale registry that makes the next language one row instead of ten files. **34** `bcac933e-153a-4ced-b894-f95b0417ec76` (`01a02f9a-ecef-7f1a-ac9e-112a11f44605`, from `d03e0723`) — numbers were formatted in the **device** locale, not the app's, so a pt-BR user on an en-US phone read `1,974` where Brazil writes `1.974`. **35** `f7a7d639-8a01-436f-9326-599096065ff1` (`01a02fac-6fa7-742c-b861-562827f4e46b`, from `72035b47`) — carries TWO commits: `d7ee0b15`, because the big number on Today is a **worklet** and so never ran the JS-thread formatter at all (it grouped with a comma whatever the locale said), and `72035b47`, a scan that hit the daily quota telling the user their photograph was the problem. The iOS half of all three is OTA 17 above. |
| iOS | **OTA number 16 on build 60** (2026-08-23) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `ec5b3e54-71b3-4e47-ba69-f8e8a454e3cf` (`01a02f5e-3cb1-76f0-a3b6-65c2f0cb0dbf`, from `02cf3470`), published from `ignia-mac`; gate `7b347b0f…` = build 60. Same JS as Android OTA 32 — Today's seven cached slices honour snapshot provenance. **Not device-verified on iOS** (no iOS device here); the Android half is verified offline. |
| Android | **OTA number 32 on vc 37** (2026-08-23) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | Update group `ae952001-75c0-4ddf-8300-e35197cff0f5` (`01a02f59-f181-748a-b06a-769138af6bab`, from `02cf3470`). **Train's defect was never Train's.** The same `fromCache` race ran through Today's SEVEN cached slices — the screen `offline-cache.ts` was written for, whose own header calls an empty day with no signal *"the worst screen in the product"*. All seven subscriptions now report a `SnapshotMeta` and every call site passes `{ authoritative: !meta?.fromCache }`; the param is optional, so the other hooks that subscribe presets/customFoods (ADR-0016) are untouched. **The trap, and why this was not a one-line change:** Today's spinner had no error exit — `loading = !snapshotArrived && !logsFromCache`. It cleared by ACCIDENT, because the empty cache hit latched `snapshotArrived`. Honouring provenance without a third clause would have traded an empty screen for a PERMANENT spinner on a cold cache offline, which is strictly worse; `failWith`/`failed` came across from `useTrain`. **VERIFIED ON DEVICE against the published bundle**: WiFi off, cold start — Today renders **1,974 kcal / maintenance 2,424**, its true current values, where the same screen offline on the previous build showed a stale **1,800**. Train re-checked on the same build and still paints its templates. `cached-state-provenance.test.ts` pins all three failure modes (discarded hydration, poisoned cache, clobbered paint) as unit tests; 345 → 350. |
| iOS | **OTA numbers 14–15 on build 60** (2026-08-23) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Final state is update group `111c3134-a06a-4a99-a9b3-f28ec04b1718` (`01a02f45-0b85-72b2-b212-ccd1d9ddf31c`, from `500cda37`), preceded by `8f510b4c…` (14, from `3f681381`) which carried the INCOMPLETE Train fix and was superseded within the hour. Same JS as Android 29–31 — the Train offline-cache work, see that row for what it actually took. Gate run on the Mac both times: `7b347b0f…` = build 60. **Not device-verified: there is no iOS test device here** — and this one earned that caveat, because the Android half needed TWO corrections that only a real screen produced. The logic is platform-neutral (a Firestore listener and an AsyncStorage read, no layout), which is the reason to expect parity, not evidence of it. Reaches TestFlight build 60 only; public iOS is on build 55, a different runtime. |
| Android | **OTA numbers 29–31 on vc 37** (2026-08-23) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **Three publishes for one fix, and the DEVICE forced both corrections — 345 green specs never saw either.** Final state is update group `19f59138-76a9-4211-a2d9-7230ee1fdbea` (`01a02f41-56e9-7169-966e-543bae6e9af6`, from `500cda37`), preceded by `3cf38427…` (29) and `b2f6c5bc…` (30). **The Train tab was the only one that could not render without the network.** Firestore is memory-only here (RN has no IndexedDB), and the AsyncStorage display cache served **Today only** — `useCachedState` was imported by `useToday` and `auth.tsx` and nothing else — so Train blocked on a server round-trip before painting anything, on EVERY focus, not just cold start. Not data volume: the heaviest account in the project is 20 sessions + 33 exercises + 3 templates = **71,485 B**. It also could not fail: `loading` started true and was cleared only in the sessions SUCCESS callback, so an errored listener span forever while `train.loadErr` sat unreachable inside `StartView`, the else branch. **Publish 29 fixed the spinner and NOT the paint** — offline with a warm cache Train still read *No templates yet* for an account with three. An offline listener fires immediately with an EMPTY result and `fromCache: true`; that latched the live guard, discarding the disk hydration, and wrote the empty array THROUGH, poisoning the cache for next time. **Publish 30 carried provenance and was STILL wrong**: not latching and not persisting left `setValue` running, so the hydrated list was replaced by `[]` a frame after it appeared — indistinguishable from a cache that was never written. Publish 31 ignores a cache-only value once anything has been painted from disk. **VERIFIED ON DEVICE against the published bundle**: WiFi disabled, cold start, Train paints Leg Day / Push Day / Pull Day from disk. `subscribeProfile` had reported provenance since it was written and `useToday` threw it away; that call site now honours it. **Today's other seven slices still have the identical race** and are deliberately not touched — unverified on a device, and Today is the screen `offline-cache.ts` exists for. |
| iOS | **OTA number 13 on build 60** (2026-08-23) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | Update group `5ced7104-7c1a-4c07-9950-627eb38526bf`, iOS update `01a02ede-eb44-7c22-aaa2-a965c3a9c6dd`, from `a35ecfcf`, published from `ignia-mac`. **The iOS half of Android OTA 28** — same two commits, same JS, published the same day so the two platforms stop diverging. Gate run on the MAC (it must be): computed `7b347b0f…` = build 60's runtime. iOS assets fall further than Android's — **4,778,400 → 607,184, −87.3%**, total −25.7% — because the 962,968-byte MaterialSymbols face `expo-symbols` drags in is Android-only in the export; iOS ships 26 assets to Android's 30. **Not device-verified: there is no iOS test device here.** The Android half IS verified in detail (row below), and it is the identical JS — but the icons/fonts change is a RENDERING change, which is exactly the class that has differed across platforms here before. Reaches **TestFlight build 60 only**; public iOS is on build 55, a different runtime, and gets none of it. |
| Android | **OTA number 28 on vc 37** (2026-08-23) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output + `unzip -p app-release.aab base/assets/fingerprint` | Update group `80c1d557-5398-4f3d-8b53-012c8466db3a` (`01a02ead-bf0b-7ea7-bba3-984717d9fa8a`, from `f7425686`). **Bundle size only — no behaviour change, so no `WHATS_NEW_VERSION` bump** (the F3–F7 banner already fired on numbers 24–27). Carries `20a62312` (Ionicons subpath) and `25ae21b9` (two Manrope weights): export **19,023,854 → 14,429,325 B, −24.2%**. The gate was run against the ARTIFACT, not the handoff's prose — computed hash and the `.aab`'s own `base/assets/fingerprint` both read `ae526937…`. **Verified on the LG G6 against the published bundle**, and the two failure modes are not alike so both were checked separately. *Icons fail loud*: all four tabs screenshotted, zero tofu. *Fonts fail quiet* — `_layout.tsx` ships system fonts rather than a blank app on a font error, so "it launched" proves nothing. Settled structurally instead of by eye: the two Manrope hashes the new bundle references (`8e8fe178…` = 700Bold, `f8f555c6…` = 800ExtraBold) are **byte-identical to fonts already embedded in the vc 37 binary** (`embeddedAssetFileMap` → `android_res/raw/…manrope_{700bold,800extrabold}.ttf`), so they resolve locally and loading cannot fail. The subpath import ships the SAME files; it only stopped bundling the other five. Device confirmed on this bundle by `downloadedManifest` = `01a02ead…` on launch 1 and `CheckCompleteUnavailable` on launch 2. |
| Android | **OTA numbers 24–27 on vc 37** (2026-08-22) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **The rest of the UX_AUDIT F-series: F3, F4, F5, F6, F7.** Final state is `f533e329-5332-48d1-bf17-3db1ed5f7c95` (from `ea0e60d7`), preceded by `a34e5dc3…`, `1dacbfa0…` and `0c9f01db…`. **F3 kilograms** — body weight AND barbell loads, in two separate core modules on purpose: per unit the *objects* differ, so the bar is 45 lb or **20 kg** (= 44.09 lb), the plates are 45/35/25/10/5/2.5 against 25/20/15/10/5/2.5/**1.25**, and the step is 5 or 2.5. **Plate math and the warm-up ladder are therefore solved in the DISPLAY unit** — a pound solve converted afterwards tells a metric lifter to load a 20.4 kg plate, and there is a test asserting exactly that. Storage stays in pounds everywhere. **F4** the speed dial says *Search foods* and the sheet has a title. **F5** the + button no longer covers *Repeat yesterday*. **F6** a "?" glossary on Today and Trends, generalised from the Train one. **F7** `CutPace` is unsigned and every consumer subtracted it, so a "gain" user got a deficit once the estimator took over. **THREE separate publishes were corrections the DEVICE forced, and two of them are the same mistake twice.** (1) `flex: 1` on Today's empty state means `flexBasis: 0` in RN, so on an account whose hero already fills the viewport the block collapsed and *Repeat yesterday* vanished entirely — `flexGrow: 1, flexShrink: 0, flexBasis: auto` is what was meant. That is the SAME shorthand that had just broken the template editor. (2) The plate panel read `WORKING SET · 100 KG` with a correct `25×1 15×1 / side` and then a warm-up ramp of `45 / 110 / 150 / 195` — pounds, under a kilogram heading. `generateWarmup` is built from `computePlateLoad` and needed the same bar and plates; the visible half was converted and the ladder two lines below it was not. **VERIFIED ON DEVICE against the published bundles**: the units row reads *Units · Serving sizes and body weight*, the Body hero reads 81.0 kg with a −0.7 kg/wk trend and an 83.5 → 78.0 kg rail, **80.5 kg typed came back out of PROD Firestore as 177.47 lb**, the set table header reads **KG**, and a 100 kg squat solves to `25×1 15×1 / side` over a `20 × 10` warm-up. Suites: core 980, mobile 340, web 204 |
| iOS | **OTA number 12 on build 60** (2026-08-22) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `97b1cdd3-9f99-4f5e-bf35-d11faf9dff97`, iOS update `01a02bd2-ff7b-7c20-9f01-aef4f05118ae`, from `b3527db7`, published from `ignia-mac`. The whole F3–F7 change in ONE publish, after Android had shaken out the three corrections. **And this one is NOT "behaviour unverified on iOS" — for the first time the JS was run on a real iOS build before publishing.** A Release SIMULATOR build from `main` was compiled on the Mac, installed on `QA-iPhone`, and the Maestro suite driven against it: **17 of 19 flows pass**, including `19-glossary` and `20-units-metric` on their first run. **The two failures are PRE-EXISTING and that was proven rather than assumed** — `27828e5c` was checked out, rebuilt, installed and run, and `15-search` and `18-train-template` fail there too. `.maestro/regression/coverage.md` has what each actually shows, taken from the run artifacts and from Firestore rather than from reasoning. **Four environment traps were hit getting there and all four are now in `DEV_ENVIRONMENT.md` §3.12–3.13**: Maestro needs Java 17+ and this Mac's `java` is 11 (Homebrew's openjdk 26 was already installed, just off PATH); `maestro test <dir>` under Maestro 2.x runs ONE flow and exits 0; a fresh install opens the guided tour, which covers Today and made the suite report *"No visible element found: Water"*; and **`-sdk iphonesimulator` fails the build on the WATCH target's AppIcon**, because it forces every target in the scheme onto that SDK — use `-destination`. Also recorded: a simulator build carries no `expo-channel-name`, so it can never take an OTA. **Reaches TestFlight build 60 only**; public iOS is build 55, a different runtime |
| Android | **OTA numbers 20–23 on vc 37** (2026-08-22) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **Every bottom sheet in the app is now `<BottomSheet>` — and it took FOUR publishes because the device rejected what 333 green tests passed. Twice.** Final state is update group `b80641f0-1665-42e8-82a1-f9c78b0ef470` (from `750e01f4`), preceded by `f660f53a…`, `877850b4…` and `ef881035…`. Six sheets moved off their hand-rolled `Modal animationType="slide"`: Train's four (starter templates, exercise detail, add exercise, finish), the template editor, and **EntrySheet** — which is where the shared component was originally invented, and whose left-behind copy had since fallen behind it, keeping its own `Keyboard` listeners and a `paddingBottom` that omits `insets.bottom` at rest (the expression that put Save under this device's 48dp nav bar). `BarcodeScanner` keeps its slide Modal and is the documented exception: a full-screen camera takeover is not a sheet. **The two device-only regressions, both layout, both invisible to jest — this is the entry to re-read before touching `<BottomSheet>`.** (1) The component splits the panel across two layers (RN-Animated and Reanimated styles cannot compose on one node) and the `maxHeight` ceiling lives on the OUTER one, while every sheet it replaced carried `maxHeight` on the PAINTED view and so bounded its own flex children for free. Without that, the meal sheet's panel kept its full content height, `formScroll`'s `flexShrink: 1` had no definite parent to shrink against, and **the Add button sat below the fold with nothing able to scroll to it** whenever the keyboard was up. Fix: `flexShrink: 1` on the panel. A percentage cannot do this job — the panel's parent is auto-height. (2) The template editor rendered as **a bare handle above an empty strip**: its `GestureHandlerRootView` was `flex: 1`, correct when it was the direct child of a full-screen Modal and fatal inside a content-height panel, where `flex: 1` means flexBasis 0. Fix: `flexShrink: 1`. Also fixes the starter-template rows sitting flush against each other — they are mapped straight into the ScrollView, so the panel `gap` (which reaches direct children only) never touched them; the gap now lives on the scroll container, matching `styles.list` on the Train screen. **VERIFIED ON DEVICE, sheet by sheet, against the published bundles**: meal entry (browse, on-device search, food detail, manual form with Add pinned above the keyboard and the fields scrolling under it), weigh-in, water (12 + 5 → 17 fl oz still writes), template editor (empty AND with three exercises, scrolling to Remove/Save), starter templates (spaced, scrolls to the fifth row), add exercise, finish. **The exercise-detail sheet was NOT individually exercised** — it is structurally identical to the starters sheet, which was. `sheets-are-one-component.test.ts` fails the build if anything hand-rolls a slide-up Modal again or calls `useKeyboardSheetPadding` outside `<BottomSheet>`. Mobile suite 330 → 333 |
| iOS | **OTA number 11 on build 60** (2026-08-22) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `d185a68d-90ac-41eb-bdb7-8fc9ec645f09`, iOS update `01a02b4c-a169-73c4-a9ea-5f8809526c22`, from `750e01f4`, published from `ignia-mac`. The same sheet sweep as the Android row, **published only after all four Android publishes had settled** — the two layout regressions were found and fixed on the device first, so iOS receives the corrected state in one go. Gate parsed as JSON on the Mac and matched build 60 exactly; Metro export run there before publishing; tree clean under `--untracked-files=no`. **Behaviour UNVERIFIED on iOS, and this is the entry where that caveat carries the most weight so far** — six sheets changed container, the two defects Android surfaced were both layout, and iOS is the platform whose keyboard geometry took four attempts on 2026-08-22. If an iOS device becomes available, the meal sheet's manual form with the keyboard up is the first thing to look at. **Reaches TestFlight build 60 only**; public iOS is build 55 (`886bf0b3…`), a different runtime |
| Android | **OTA number 19 on vc 37** (2026-08-22) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | update group `b24c3858-715f-48df-bfee-597f544eddea` (`01a02b10-2a67-7ae2-9e26-9dc85b80961c`, from `b8b1abfd`). **The owner asked whether logging a weight works the same way as logging water. It did not — and the two had drifted in OPPOSITE directions, each holding half of what the other lacked.** Water and Sleep hand-rolled `<Modal animationType="slide">`, which slides the dim backdrop UP THE SCREEN with the panel (the "weird backdrop" `EntrySheet` was rebuilt to avoid) and has no drag-to-dismiss; the weigh-in sheet used the shared `<BottomSheet>`, which fixes both — so water and sleep moved onto it, deleting their duplicated backdrop/sheetWrap/sheet/handle styles. Going the other way, water had `selectTextOnFocus`, `returnKeyType="done"` and a note line saying what the total was about to become; the weigh-in sheet had none of them **and its field is PREFILLED**, so editing 180.4 → 179.8 cost five backspaces and typing without clearing produced `180.4179.8`. Sleep had the same hole. **NOT copied: water's Add/Set toggle** — water accumulates and a weight replaces, so "add 5 lb" means nothing; and the keypads stay different (`number-pad` for whole fluid ounces, `numeric` for a decimal weigh-in). The RULE is what is shared, not the value. **VERIFIED ON DEVICE against the published bundle**: the weigh-in sheet reads *"Last weigh-in: 178.5 lb"* at rest (correctly NOT "Today" — no weigh-in today), *"178.5 → 179.2 lb"* once typed, and **"Enter a weight between 50 and 500 lb." in red with Save disabled** for a 12 — that state previously greyed Save out and said nothing at all, which reads as a broken button. Backdrop-tap dismiss works on the new container, and water still writes correctly through it (12 + 5 → **17 fl oz** on the row). **Caveat on the device evidence:** `adb input swipe` does not produce the incremental move events `PanResponder` needs, so **drag-to-dismiss could not be exercised over adb** — it is asserted only by `<BottomSheet>` being the same component the weigh-in sheet has always used. Mobile suite 321 → 330 |
| iOS | **OTA number 10 on build 60** (2026-08-22) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `ca728718-65ab-4525-ae13-4d5b77e43d47`, iOS update `01a02b18-0083-7b69-b69d-31b08b780283`, from `b8b1abfd`, published from `ignia-mac`. Same change as the Android row above. Gate run on the Mac first and it matched build 60's `.ipa` exactly — **and the JSON was parsed ON THE MAC this time, because piping the raw dump home and `head`-ing it broke the pipe and printed 20 KB of `sources` into the session; the first `hash` in that dump is `4209d5b8…`, which is the trap the row for OTA 7–8 already warns about.** Metro export run on the Mac before publishing. `git push` done first; Mac tree clean under `--untracked-files=no`, so the published JS is exactly `b8b1abfd`. **Reaches TestFlight build 60 only** — public iOS is build 55 (`886bf0b3…`), a different runtime. **Behaviour UNVERIFIED on iOS**; the Android half is device-verified above. Worth an iOS eye when one is available: the keyboard-sheet geometry this touches is the one that took four attempts on 2026-08-22, and water/sleep changed containers |
| Android | **OTA numbers 17–18 on vc 37** (2026-08-22) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **UX_AUDIT F1/F2 — the calorie seed stops being blind to sex, height and age.** Final state is update group `331c3a97-d4c2-44c5-9055-8d689a5a5284` (`01a02afc-a405-71c2-b95e-469498a95230`, from `4a48e633`), preceded by `bbbff7fd-002c-40ab-912c-bba38cd8c550` (`01a02aef-08ea-7ba2-8581-17ac211333ff`, from `30e6305c`). Onboarding used to seed the target with `computeKcal` = weight × {11|14|17} — no sex, height, age or activity — so at 180 lb / 45 y a woman's "lose fat" target of 1,980 kcal sat **2 kcal/day ABOVE her own estimated maintenance** while a man of the same weight and age lost at 0.69 lb/wk. Two skippable steps (body: sex/height/age · activity: the five Refine buckets) now feed `onboardingSeed` in `packages/core`. Number 18 additionally drops the stray rule above **Delete account** — the owner spotted it mid-verification; the card has exactly one row, so the divider separated nothing (same defect as `refineRow`-as-`soloRow` earlier the same day). **VERIFIED ON DEVICE against the published bundles, end to end, INCLUDING the Firestore write** — which is the half a screenshot cannot show. On the LG G6: six progress dots instead of four (the cheapest proof the new bundle is live), the body step renders whole at 360×720dp with nothing under the CTA, the plan step reads *"Based on your height, age, sex and activity — we estimate you burn about 2,748 kcal a day"* against a 2,350 target, **Skip for now** jumps to the plan and drops the dots to four, and Back un-skips onto the body step rather than the activity one. Then the write itself, read back out of PROD Firestore: `sex`/`heightIn`/`age`/`activityLevel` all stored, `targetPaceLbsPerWeek` **0.8 — the value the account already had, NOT onboarding's own default of 1** (the redo-overwrites-your-pace trap), `manualCaloriesTarget` 2,350 rather than the heuristic's 1,980, and `targetsRefinedAt` **stamped** instead of deleted. **No `firestore.rules` deploy was needed and that was proven, not assumed** — three new cases in the rules spec plus this live accepted write. `WHATS_NEW_VERSION` deliberately NOT bumped: this reaches existing users only if they open Settings → Edit goals, and re-firing yesterday's banner would nag anyone who just dismissed it |
| iOS | **OTA number 9 on build 60** (2026-08-22) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `7c67e976-85be-42be-97bd-68c0776aa578`, iOS update `01a02b00-b87e-7eaf-a191-e150db1ee9d9`, from `4a48e633`, published from `ignia-mac`. Same change as the Android row above — the F1/F2 calorie seed plus the Delete-account divider — in ONE publish, because Android had already shaken out the corrections. Gate run on the Mac first and it matched build 60's `.ipa` exactly (**parsed as JSON and read off `.hash`; a regex would have returned `4209d5b8…`, the first entry of the `sources` array — that trap cost ~30 min the previous session**). Every commit in the range is `.ts`/`.tsx` plus docs, so neither runtime could move. Metro export run on the Mac before publishing, into `/tmp` and deleted after; disk was 5.8 Gi free. `git push` done first, `origin/main..HEAD` **0**. The Mac's tree showed dirty (`*` on the commit) from untracked artifacts; `git status --untracked-files=no` was empty, so the published JS is exactly `4a48e633`. **Reaches TestFlight build 60 only.** Public iOS is build 55 (`886bf0b3…`), a different runtime. **Behaviour UNVERIFIED on iOS** — the Android half of the same JS is device-verified in detail above, including the Firestore write |
| iOS | **OTA numbers 7–8 on build 60** (2026-08-22) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | **Number 8 is `e65c0ff2-1c42-42dd-ade9-7821ff7c6349` (`01a02a82-c907-70b7-a588-9a1dd51cfe64`, from `c1536024`) — the iOS keyboard-padding fix**, reported as *"spacing is much larger"* on most input screens. Two causes: onboarding double-counted the home indicator (`SafeAreaView` reserves `insets.bottom`, then `behavior="padding"` adds a keyboard height measured FROM THE SCREEN BOTTOM that already contains it — cancelled with `keyboardVerticalOffset={-insets.bottom}`, and it is the only one of the five carrying the `bottom` edge); and all five screens now take `KeyboardAvoidingView` from **react-native-keyboard-controller** rather than react-native, whose own version reads the system keyboard frame directly and is at the mercy of the iOS 26 reporting inconsistencies Apple documents in forum threads 800310 and 814154. `behavior` stays iOS-only — Android relies on `windowSoftInputMode=adjustResize` and **was verified unregressed on the LG G6 after the swap**. **PRE-EXISTING, not caused by that day's work**; none of the five screens had changed. Numbers 7 = update groups `e68cda6d-b6ca-4de4-b9d7-c31c9e3cb14c` (iOS update `01a02a4e-dbee-7df0-ad0c-d6e98680c4e6`, from `c3651684`) and **`381a4408-8ab6-4b16-8a5d-5b84c68c0699`** (`01a02a65-6f5c-76e5-9bdb-9784fb08b4eb`, from `f2876c36`), published from `ignia-mac`. **The guided tour** (`UX_AUDIT` F0 — a real user asked to be shown the app) plus the bottom-sheet keyboard fix the owner reported with a screenshot. Two publishes because the first shipped the tour inside the tab layout and with the old sheet lift; the second carries both corrections. **A GATE TRAP WORTH RECORDING:** the first read of the gate looked like the iOS fingerprint had moved to `4209d5b8…`, which would have meant a stranded channel and a forced binary before the Apple migration. It had not moved — `4209d5b8…` is the hash of the FIRST ENTRY in the `sources` array, and a regex for `"hash":"…"` over the raw JSON matches that before it reaches the top-level `hash`. **Parse the JSON and read `.hash`; never regex it.** The real value is `7b347b0f…`, build 60's exact runtime, confirmed at HEAD and again at build 60's own commit `f0130b01`. `git push` done first. Mac tree clean under `--untracked-files=no`. **Behaviour UNVERIFIED on iOS**; the Android half of the same JS is device-verified in detail below. TestFlight build 60 only — public iOS is build 55, a different runtime |
| Android | **OTA numbers 12–16 on vc 37** (2026-08-22) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **Number 16 is `aa4ba332-f131-4515-b874-46eac01c2a48`** — the Settings stray-divider fix plus the keyboard-controller swap (inert on Android by design; verified so on device). `refineRow` carries a top border because it sits UNDER *Edit goals* in the Goals card; reused as the ONLY row in the new Help card and in Send feedback it drew a rule across the top of each — `soloRow` is the same metrics without it, and the feedback one predated the tour, it just had no neighbour to make it obvious. Numbers 12–15: **The guided tour, and four publishes to get two screens right — every correction came from the DEVICE, none from the suite.** Final state is update group `b8111f48-cb83-42ad-84d9-579fbdb89e2f` (`01a02a60-cf63-7f5a-b36d-92c289a3886c`, from `f2876c36`), preceded by `46cc182b…` (tour), `244078af…` and `ad57fd3b…`. **The tour** answers the only concrete thing in a second-hand report: *"she needs some kind of tutorial or something. Like a walkthrough of the app."* Six cards, auto-opens once per device for anyone with a completed profile, skippable and replayable from Settings → How Ignia works. **Publish 1 rendered and was wrong three ways on a 360×720dp screen**, all invisible to `jest`: the fifth row of the map card was absent (the ScrollView ended flush against the pinned CTA), *Skip* rendered as *Skin* (a fixed 44px top bar cropped the descender), and the footer collided with the tab bar and the raised + button. The last one is structural — a guided takeover cannot render inside the chrome it is explaining — so the tour became a **root route** and `AuthGate` learned about it, since the gate bounces any non-`(app)`, non-onboarding route straight back to `/(app)`. **The sheet keyboard fix took two goes and the second corrects the first.** Removing the dead `insets.bottom` padding closed the gap the owner photographed on iOS and immediately drove Save behind the keyboard on the G6 — because `useKeyboardSheetStyle` was ALSO nudging the sheet down by `insets.bottom - 8`, documented as correcting an overshoot that **measurement shows does not exist**. Two errors in opposite directions had been cancelling. The nudge is gone; sheet bottom now sits on the keyboard top and the primary button's clearance is the sheet's own padding. **VERIFIED ON DEVICE end to end**: all six tour cards, the completion exit, no re-open after a full restart, and water 5 + 7 → **12 fl oz** written with Save fully clear of the keyboard |
| iOS | **OTA #6 on build 60** (2026-08-22) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` update group `919560f8-5964-4c1a-bef5-e84d3c39feca`, iOS update `01a0298a-de4b-7cea-ace4-aec88779d606`, from `c91eff51`, published from `ignia-mac`. Same change as the Android row below — water entry plus the bottom-sheet clipping fix. Gate run on the Mac first and it matched build 60's `.ipa` exactly; every commit is `.ts`/`.tsx`. `git push` done first, `origin/main..HEAD` **0** before gating. Mac tree clean under `--untracked-files=no`, so the published JS is exactly `c91eff51`. Disk on the Mac is back to **10 Gi free** (it was ~600 Mi yesterday). **Behaviour UNVERIFIED on iOS**; the Android half is device-verified in detail. TestFlight build 60 only — public iOS is build 55, a different runtime |
| Android | **OTA numbers 8–11 on vc 37** (2026-08-22) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | **Four publishes for one feature, and the iterations are the point.** Final state is update group `845e418a-17dd-464d-80e0-d2f9683ae8f5` (`01a0297e…`, water entry) plus `01a02985-6e1f-7111-be5f-967a478ce8fd` (sheet clipping), from `c91eff51`. **Water takes an exact amount** — the first report through the feedback box that shipped that morning, ~10 h after it went live: *"no hay un opcion customizada cmo poner 5oz"*. The +8/+16/+24 pills stay as the fast path; the water VALUE opens a sheet with one field, a live `0 → 5 fl oz` preview, and a small *Set total* link on the title row. **The first two publishes were worse and were caught by LOOKING at the device, not by tests** — the suite was green through all four. Build 1 had a segmented Add/Set control AND quick chips AND the field, three choosers stacked to log a glass of water, with the chips duplicating the pills two inches above and Save pushed under the keyboard. Build 2 dropped the chips but left the mode link below Save, i.e. behind the keyboard, which is the same as absent. Build 3 moved it to the title row. Build 4 fixed a **pre-existing** clipping bug the owner spotted: `styles.sheet` used a fixed `paddingBottom: 32` while this device's software nav bar is 48dp, so the Save button lost ~16dp and missed taps near its lower edge — `SleepModal` shares that style and had it too, since it was written. **VERIFIED ON DEVICE end to end**: typing 5 against a 0 total wrote 5 fl oz to the row, and the `−8` pill appeared with it. RNTL never runs a Yoga layout pass (`jest.config.js` says so), so this whole class is screen-only |
| iOS | **OTA #5 on build 60** (2026-08-22) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `c4339b05-942d-460e-a7c9-0ebaecb28250`, iOS update `01a02760-a71c-7f59-852d-01477fe56f1e`, from `fe83f713`, published from `ignia-mac`. **Carries the whole 2026-08-22 change in ONE publish** — the speed-dial fix, custom targets and in-app feedback, including the stuck-save fix Android needed a second OTA for. Gate run on the Mac first and it matched build 60's `.ipa` exactly; every commit in the range is `.ts`/`.tsx` plus docs, so neither runtime could move. Metro export (`expo export --platform ios`) run on the Mac before publishing, into `/tmp` and deleted after — **the Mac is at 100% disk with ~600 Mi free**, which is enough for a 17 MB export and not much else. `git push` done first and `git rev-list --count origin/main..HEAD` confirmed **0** before gating. The Mac's tree showed dirty (`*` on the commit) from untracked artifacts; `git status --untracked-files=no` was empty, so the published JS is exactly `fe83f713`. **Published while 1.2.1 sat in `WAITING_FOR_REVIEW`** — a deliberate owner decision, not an oversight: the standing caution is not to change the app while a reviewer has it open, and `WAITING_FOR_REVIEW` means queued rather than opened. **Reaches TestFlight build 60 only.** Public iOS is build 55 (`886bf0b3…`), a different runtime — do not report this as shipped to users. **Behaviour UNVERIFIED on iOS** — no iOS device QA; the Android half of this same JS is verified on device in detail |
| Android | **OTA #7 on vc 37** (2026-08-22) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | update group `2e0b017b-b98f-4682-b34c-9ff83f6db7e6`, android update `01a0275a-91ee-7a42-8edb-3b7eb8f1e6ad`, from `fe83f713`. **Exists because OTA #6 shipped a bug that only device use could find.** The targets editor cleared its `busy` flag only in `catch`, which relies on `router.back()` unmounting the screen; when it did not, the Save button was left spinning and `disabled` forever, every later tap silently ignored and no write ever issued. On the G6 that read as a failed save — the toggle said Automatic while Firestore still said `custom`. `finally` on both save paths. Also formats the validation copy with `toLocaleString`: the steep-cut warning printed "2723 kcal" directly above a note printing "2,723 kcal". **Verified on device**: the warning now renders `2,723`, the Save button is live, and switching to Automatic wrote `targetMode: 'auto'` while LEAVING `manualCaloriesTarget: 2000` / `manualProteinTarget: 165` stored — which is the whole claim `targetMode` exists to make true, measured rather than asserted |
| Android | **OTA #6 on vc 37** (2026-08-22) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | update group `8193ea52-2dca-4dac-96a2-763753eb3c72`, android update `01a0274c-29b9-784d-b85e-9d4a014fafac`, from `5271c984`. **Abdiel Medina's three items** (`UX_AUDIT.md`): the speed-dial dismissal fix, custom calorie/protein targets behind an explicit `targetMode`, and the in-app feedback composer. Gate run BEFORE publishing and it matched vc 37's `.aab` exactly; Metro export re-run after the last edit. `WHATS_NEW_VERSION` bumped to `2026-08-22-your-numbers` — this one IS user-facing, unlike OTA #5. **VERIFIED ON DEVICE, against the published bundle rather than ahead of it**: the G6's own `dev.expo.updates` log carries both ids, then, one by one — tapping the "Scan meal" LABEL navigated (it did nothing before), hardware back on the scan screen dismissed the dial and stayed put (it used to land on Today with the dial open), the What's-new copy and its feedback line rendered, a typed 2,000 moved the hero ring off the measured 2,323, a below-floor 1,300 was refused with Save disabled, and a real feedback send reached `gabriel@bermudezsystems.com` — confirmed `delivered` by the Resend API, which proves client → rules → trigger → mail end to end. **Carried one bug, fixed in OTA #7 above** |
| Android | **OTA #5 on vc 37** (2026-08-21) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | update group `0cdd613d-0959-4aec-aa7a-66b48e2d675a`, android update `01a026f5-736d-7615-aa78-adc4d2c6607e`, from `68c582be`. **Tier D — food search now runs ON DEVICE.** A 13,272-food compact index ships in the bundle and `searchFoods` is no longer called for text; barcode still goes to the server and must (an OFF product is a live lookup). Possible only because `searchFoods` stopped consulting OFF for text on 2026-08-19, so the server had nothing the bundle lacks. Gate run BEFORE publishing and it matched vc 37's `.aab` exactly; Metro export re-run after the last edit. **Cost, measured, not estimated:** index 1,385 KB raw / **334 KB gzipped**, and the Android bundle goes **11,095,849 → 13,140,531 bytes (+2.0 MB, +18%)**. Shipping the index as a JSON string instead is WORSE (13,961,046) — recorded in `localFoodSearch.ts` so it is not retried. **The ranking is pinned against drift**: it exists in both `functions/src/usda-db.ts` and `packages/core/src/usda-search.ts` and cannot exist once, so `usda-search-golden.json` fixes the exact ordered ids for 24 queries and both copies are asserted against it; `npm run doctor` fails on a stale index or a drifted fixture. `WHATS_NEW_VERSION` deliberately NOT re-bumped — it was already set to `2026-08-21-speed` hours earlier by OTA #4 and re-firing would nag anyone who just dismissed it. |
| Android | **OTA #4 on vc 37** (2026-08-21) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | update group `6be4b564-d070-4b33-83f2-fe007eb39947`, android update `01a0264a-093e-7c19-b5f0-e325bc7a1713`, from `4b5ae6de`. **The latency work.** Client half: 768 px upload resize (down from 1080 — Gemini billed the same 1,009 input tokens either way, so the extra pixels only travelled), named scan-progress steps, `?fields=` on the barcode lookup (145.8 KB → 2.4 KB), an on-device barcode cache, and the OFF **404 → `FOOD_NOT_FOUND`** fix (v3 answers 404 for an unknown product and both clients rejected every non-2xx before parsing, so the most common barcode outcome told users to re-scan a barcode that read fine). Gate run BEFORE publishing and it matched vc 37's `.aab` exactly. **VERIFIED ON DEVICE, visually, not just published — including the barcode path, offline** (the strongest evidence in this table). Photo scan:  — the LG G6 fetched manifest `01a0264a`, restarted onto it, and a real library-photo scan rendered the NEW analyzing screen: the captured frame, “Getting the photo ready” checked in teal, “Reading the plate” spinning, “Looking up the foods” pending — then a correct review (484 kcal, `roasted potato +3`, USDA-matched). `WHATS_NEW_VERSION` bumped to `2026-08-21-speed` and the banner **fires correctly**, verified on Today with the new copy. An earlier note here said it did not fire — that was measured on the OLD bundle, one minute before the device restarted onto the update, and it was wrong. Check WHICH bundle is running before calling a JS behaviour broken after an OTA. **Barcode, verified END TO END by the owner on the device:** a real product scanned and prefilled the entry sheet from Open Food Facts (*Organic Cranberry Health Mix*, 170 kcal, 5P/13C/12F). Then the decisive cache test — **Wi-Fi turned OFF, same product re-scanned, and it still resolved.** `dumpsys wifi` confirmed `Wi-Fi is disabled` at the time, so that lookup came from AsyncStorage with no network available: the on-device cache is proven, not assumed. Wi-Fi restored afterwards (`svc wifi enable`, OFF reachable at 141 ms RTT). **A caution for whoever drives this device next:** an `adb input keyevent KEYCODE_BACK` sent to tidy up closed the scanner during the owner's FIRST scan, wasting it — do not send input while a human is using the phone. **iOS was published the next session** — see the iOS OTA #4 row directly below; at the time of writing `ignia-mac` was out of disk (116 Mi free) and the iOS gate could not even be run |
| iOS | **OTA #4 on build 60** (2026-08-21) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `d83c9d01-2e67-41d7-848e-41d953eddc7c`, iOS update `01a02696-839e-7434-99c5-e265178bbd82`, from `cb914e33`, published from `ignia-mac`. Same change as the Android OTA #4 row — **the latency work**. Gate run on the Mac first and it matched build 60's `.ipa` value exactly; every commit in the range is `.ts`/`.tsx`, and `git diff --stat` over `package*.json`, `app.json`, `plugins/`, `modules/` and `targets/` was **empty**, so neither runtime could move. Metro gate (`expo export --platform ios`) run on the Mac before publishing. `WHATS_NEW_VERSION` is already `2026-08-21-speed` from the Android publish, so iOS testers get the same banner — deliberately NOT re-bumped. **Why this lagged Android by a session, and the trap to remember:** the Android OTA publishes from the *Windows* workstation, which needs no push, so all ten latency commits sat on **local `main` only** — `origin/main` was still at `0f9ccfa5` and the Mac's `git pull` reached none of it. `git push` is a hard prerequisite for every iOS OTA and for nothing on Android; a Mac that pulls cleanly and gates green will happily publish **stale JS** and exit 0. Check `git rev-list --count origin/main..HEAD` before gating. **Reaches TestFlight build 60 only.** Public iOS is build 55 (`886bf0b3…`), a different runtime, and gets this when 1.2.1 clears review — do not report it as "shipped to users". **Behaviour UNVERIFIED on iOS** — no iOS device QA; the Android half of this same JS is verified on device, including the offline barcode cache |
| Android | **OTA #3 on vc 37** (2026-08-21) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | update group `1a1168fa-116e-4d5b-8681-9c1d72ea8fed`, android update `01a0240e-7d5d-7e50-9ada-8e841113c8a9`, from `c10d5422`. The Train session reducer (seven `useTrain` callbacks → one `dispatch` over a pure `applySessionAction` in core), the `TdeeResult` discriminated union, and `useCoreSnapshot` behind five hooks. **Gate run BEFORE publishing and it matched vc 37's `.aab` exactly.** **VERIFIED ON DEVICE, and this is the first OTA whose verification ran against the published code rather than ahead of it** — the LG G6 pulled `01a0240e` (`isUpdateAvailable=false` after a server check proves it launched), then `16-train-terms` and `18-train-template` both passed, exit 0, zero failures. 16 is the flow that matters here: it drives add-exercise, the RIR picker (a deferred `patchSet` asserted back on screen), set-kind and discard — the whole `dispatch` path. 18 covers the template editor, which this change did NOT touch. `WHATS_NEW_VERSION` NOT bumped: nothing here is user-facing |
| iOS | **OTA #3 on build 60** (2026-08-21) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `e04af5fd-0203-41fe-a17f-c576dc76dd29`, iOS update `01a0241b-1e59-7d3d-9ff0-5a93b96d66ba`, from `c10d5422`, published from `ignia-mac`. Same change as the Android row, published only AFTER the G6 run passed. Gate run on the Mac first and it matched build 60's `.ipa` value exactly — every commit is `.ts`/`.tsx`, so neither runtime moved. The Mac's tree showed dirty (`*` on the commit) from untracked `.ipa` and prebuild `Info.plist` files; `git status --untracked-files=no` was empty, so the published JS is `c10d5422`. **Public iOS (build 55, `886bf0b3…`) is a different runtime and does NOT receive this** |
| Android | **OTA #2 on vc 37** (2026-08-20) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | update group `fa69a969-4354-4cdc-9aab-ab77f3667e45`, android update `01a020f9-6629-71cf-ae3e-d9af64d343b0`, from `bda83ea0`. The estimator follow-ups: precision gate on the recalibration card, window widening (63 then 84 logged days) instead of acting on an interval wider than 250 kcal, and the `holding` line on Today. **Second OTA of the same day, deliberately** — the soak was cut short because a 12-tester alpha has almost no statistical power to find a 1-in-N crash, and all three changes are stability-positive. **Verified on device**: both ids in the G6's `dev.expo.updates` log, Today renders. `WHATS_NEW_VERSION` NOT re-bumped — yesterday's banner already tells this story and re-firing it would nag anyone who just dismissed it |
| iOS | **OTA #2 on build 60** (2026-08-20) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `ffcf0a40-29dc-4f48-a11d-0fd7de7ea145`, iOS update `01a020fb-1325-7dd6-941c-c7870f6f5967`, from `bda83ea0`, published from `ignia-mac`. Same change as the Android row. Gate run on the Mac first and it still matched build 60 — all three commits are `.ts`/`.tsx`, so neither runtime moved |
| Android | **OTA on vc 37** (2026-08-20) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | update group `e331a9d5-55b9-4d02-b9f9-127b35f74cb5`, android update `01a02056-ee73-76ed-8b5f-e2be541ab6bd`, branch `production`, from `e838dd3e`. **The maintenance fix** (`145221e2`): measured TDEE is now computed per contiguous-logging run and pooled by `1/SE²`, so a gap in the log no longer lets the days just after it set the whole number. Gate run before publishing and it matched vc 37's `.aab` value exactly. **Verified ON DEVICE, not just published** — both the group and the update id appear in the LG G6's own `dev.expo.updates` log, so it downloaded and launched rather than merely existing on the server. Same runtime as vc 37, so it reaches the alpha fleet on next launch |
| iOS | **OTA on build 60** (2026-08-20) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `022aa8c9-82c1-481c-9cac-6e0e563541f5`, iOS update `01a0205c-b21c-7151-b15f-891c3347e59b`, branch `production`, from `e838dd3e`, published from `ignia-mac`. Same change as the Android OTA above. Gate run **on the Mac** before publishing and it matched build 60's `.ipa` value exactly. The Mac's tree showed dirty (`*` after the commit) — untracked `.ipa` artifacts and prebuild-generated `Info.plist` files only, no tracked modifications, so the published JS is `e838dd3e`. **Public iOS (build 55, `886bf0b3…`) is a different runtime and does NOT receive this**; it arrives there when 1.2.1 releases |
| Android | **vc 37** (2026-08-19) | `ae526937893adb7f7349321b05caf2732da9658b` | **read from the `.aab`** | `main` (`4ec7d2d7`), alpha, 1.2.1 — **exists only to undo a fingerprint cost, and it reopens BOTH channels.** The ABI cut (`1ddb51fa`) wrote `reactNativeArchitectures` into `plugins/withGradleJvmArgs.js`, whose file *contents* are hashed (`expoConfigPlugins`), so an Android-only tweak also moved **iOS** off build 60's `7b347b0f…` — and build 60 is the binary in App Store review. Restoring the plugin byte-for-byte returns iOS to `7b347b0f…` (measured on `ignia-mac`) and Android to `ae526937…`, hence this binary. The ABI cut is preserved, rehomed to step 1b of `patch-android-release.mjs` → the **gitignored** `android/gradle.properties`, which is not a fingerprint source; the `.aab` carries exactly `base/lib/arm64-v8a` + `base/lib/armeabi-v7a` at 66 MB, so the rehome is proven from the artifact, not inferred. `BUILD SUCCESSFUL in 8m 56s`; verifier green on all five checks. **`eas submit` FAILED and exited 0** — `Google Api Error: This Edit has been deleted` after `Fastlane supply failed`, Play's edit expiring mid-upload of the 66 MB bundle; the androidpublisher API still read vc 36, which is how it was caught, and the retry landed. A fourth distinct way that command lies. **Behaviourally identical to vc 36** — nothing to re-QA that vc 36 did not already need. **vc 36 is now an orphan runtime** |
| Android | **vc 35** (2026-08-19) | `ae526937893adb7f7349321b05caf2732da9658b` | **read from the `.aab`** | `main` (`d1cc34ec`), alpha, 1.2.1 — **reopens the Android OTA channel**, shut since the 1.2.1 marketing bump (`f33f9a60`) moved the tree off vc 34's `b6f97259…`. Carries the template-editor scroll fix, the hero-ring rework, `FEATURES.tips=false` embedded, and the "Add exercise…" spacing fix. `BUILD SUCCESSFUL in 16m 4s` on the Windows workstation; verifier green (signer `CN=Macro Log Dev`, `expo-channel-name` production, both quick-add services). Live on the alpha track — confirmed from the **androidpublisher API**, while `eas submit` sat on `- Submitting` throughout and told you nothing. **Behaviour UNVERIFIED**; `18-train-template` is the flow to re-run first, since vc 35 is what fixes the bug it dies on. Three launch attempts failed first and ALL exited 0 — see `build-android/REFERENCE.md`, *Three commands that FAILED and exited 0* |
| Android | **OTA on vc 35** (2026-08-19) | `ae526937893adb7f7349321b05caf2732da9658b` | `eas update` output | update group `b6f1f303-e4ca-4fb0-bd42-5446aa9a1032`, branch `production`, from `6b052378`. Food-search latency: servings now ship with each search hit so tapping a result makes no `getFoodDetail` call (that callable is separately cold and fired exactly once per log, so it never warmed — 2.83–3.79 s, every time). Same runtime as vc 35, so it reaches the alpha fleet on the launch after they install it. **The server half needed no client at all** and is live for everyone on every platform |
| iOS | **OTA on build 60** (2026-08-19) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | `eas update` output | update group `baa41698-0d79-4b6c-a835-fb5027d8a733`, branch `production`, from `8b69c606`, published from `ignia-mac`. Same change as the Android OTA above; same runtime as build 60, so no binary was needed. Gate run on the Mac before publishing and it matched build 60's `.ipa` value exactly |
| Android | **vc 34** (2026-08-18) | `b6f97259a3c21c116602159fb0848d2b846b03da` | **read from the `.aab`** | current `main` (`2b06a375`), alpha, 1.2.0 — **the binary that reopens the Android OTA channel**, shut since 08-17 when `plugins/withGradleJvmArgs.js` moved the hash. Carries the whole Train template rebuild. Verifier green: signer `CN=Macro Log Dev`, `expo-channel-name` production, both quick-add services through the manifest merge. `BUILD SUCCESSFUL in 22m 21s` on the **Windows** workstation. Two things cost time and are worth knowing: `expo prebuild --clean` deletes `android/local.properties`, and this box's exported `ANDROID_HOME` points at a directory that **does not exist** — the real SDK is `Z:\packagesndroid-sdk`, a short path chosen to clear MAX_PATH — so Gradle fails with "SDK location not found", which reads like a missing install. The first Play submit also died on a transient `Some of the Android App Bundle uploads are not completed yet`; alpha was still vc 33, so the retry was safe and worked. **Behaviour PARTLY VERIFIED 2026-08-19** on an LG VS988 (LG G6, Android 9 / API 28) over adb — the Maestro suite now has an Android host. Suite **14 of 17**; open: `06-scan-intro`, `15-search`, `18-train-template`. The hero rings render and animate correctly here, which is what proved the iOS ring bug was not cross-platform. **Google Sign-In on this Play-signed install FAILS** with `ApiException: INTERNAL_ERROR` (8) — not `DEVELOPER_ERROR`; the vc 34 signing cert (`1483ddc3…`, the *previous* Play key), its OAuth Android client, the `webClientId`, clock and network all check out, and the failure is inside Google's own picker. Captures were NOT collected, so `coverage.md` rows are deliberately not flipped |
| Android | **OTA on vc 34** (2026-08-19) | `b6f97259a3c21c116602159fb0848d2b846b03da` | `eas update` output | update group `c8b3b423-a49b-4094-9dda-716a3cb2587f`, branch `production`, from `735d9b3d`. **Pauses the tip jar** (`FEATURES.tips=false`) while operations transfer to Bermudez Systems LLC — see `STATUS.md` §3. Same runtime as vc 34, so it reaches the whole alpha fleet on next launch. The matching **iOS publish LANDED** the same day — update group `c8b3b423-a49b-4094-9dda-716a3cb2587f`, runtime **`886bf0b3…`**, i.e. the runtime the live App Store **build 55** ships, so public iOS users are gated off. This cell said the iOS publish was PENDING until 2026-08-19; `eas update:list --branch production` disproved it. **Still open:** that OTA went ONLY to `886bf0b3`. TestFlight **build 58** runs `b3c50e91…` and never received it, so 58 testers still see a tip button for consumables that are `DEVELOPER_REMOVED_FROM_SALE`. The 1.2.1 binary embeds `FEATURES.tips=false`, which closes it |
| iOS | **OTA on build 58** (2026-08-18) | `b3c50e914fd4e477e36c486bf3ff189a157da586` | `eas update` output | update group `66e6f663-61d1-4b62-a6d4-b9942d2bf420`, branch `production`, from `1fc9bbb1`. Same runtime as build 58, which is the point — **adding `react-native-sortables` did NOT move the fingerprint**, because it is pure JS and autolinks no native module, so the drag-to-reorder work shipped without a binary. Build 58 was added to **Public Beta Testers** the same day (57 was the group's newest before that), so the update reaches them on the launch after they install 58. Verified before publishing: `Sortable.Flex` mounts and the template editor renders inside it, on the simulator, driven by Maestro |
| Android | **vc 33** (2026-08-17) | `c1c010ac57c8a0bf7fc416f4bf59c7aca045255f` | **read from the `.aab`** | current `main`, alpha, 1.2.0 — **replaces vc 32**, which was orphaned by CRLF normalization (see the line-endings section). Carries the preset/custom-food write fix. Verifier green; `BUILD SUCCESSFUL in 25m48s`. Built from a fully LF-normalized tree, so this hash is reproducible from the commit — unlike vc 32. **Superseded as an OTA target the moment `plugins/withGradleJvmArgs.js` changed** (Gradle memory fixes, same day): the plugin list is hashed, so the tree now reads `641cf5d4…` and no Android OTA reaches vc 33 until the next build. **Behaviour UNVERIFIED** — still no device QA on any SDK 57 Android binary |
| iOS | **build 60** (2026-08-19) | `7b347b0f24ace4cdc1fb24c7f972f67768641161` | **read from the `.ipa`** | `main` (`f0130b01`), **1.2.1** — the first binary off the 1.2.0 version string. Built LOCALLY on `ignia-mac` (`eas build --local`), zero EAS quota, verifier green (all four targets, both `.lproj`, Live Activities, appex entitlements). **Why 1.2.1 exists at all:** builds 55/57/58 ALL carry `1.2.0`, and an App Store version only accepts builds whose version string matches it, so no TestFlight build could ever be promoted — shipping SDK 57 required a fresh marketing version, not a promotion. Carries the iOS hero-ring fix (`react-native-svg` does not receive Reanimated animated-prop updates under Fabric), `FEATURES.tips=false` embedded (build 58's runtime never got that OTA), and the template-editor spacing fix. **Behaviour UNVERIFIED** until the rings are confirmed on a real device |
| iOS | **build 58** (2026-08-18) | `b3c50e914fd4e477e36c486bf3ff189a157da586` | **read from the `.ipa`** | current `main` (`f432b301`), 1.2.0 — **the binary that reopens the iOS OTA channel.** Build 57's runtime had been unreachable since the `withGradleJvmArgs.js` change (see its row); this one is built FROM that tree, so its fingerprint equals the gate's and a JS fix can reach it over the air. Carries the two shipped layout fixes found by the Maestro sweep — the body-fat percentage rendering outside its card, and Today's list having no bottom padding under the tab bar — plus the RIR VoiceOver label. Built locally on `ignia-mac` in ~24 min; slower than the usual 13–16 because `DerivedData` was empty and the desktop session was contending for CPU (load average 72, DisplayLink compositing at 84%). Verifier green on all four targets. **On TestFlight, `VALID`** (uploaded 2026-08-18T11:37:45-07:00) — the iOS OTA channel is reopened. It took two attempts, and **the first one is the lesson**: `eas submit … --non-interactive` launched at 12:28 **hung at `- Submitting`** and was still alive 1h35m later at `0:10.90` CPU, state `S`, with 58 absent from `/v1/builds` entirely — against a documented ~3 min submit. The stall is silent twice over: the invocation piped through `tail`, so nothing printed, and killing the pipeline made it **exit 0**, which a background-task watcher duly reported as success. A re-run from the same `.ipa` uploaded fine. **Poll ASC for the build; never read the submit CLI's exit code** — `REFERENCE.md` already says it has lied in both directions, and this is a third way. Run it detached (`nohup … &`) so a closing SSH session cannot signal it. Note the CLI kept printing `- Submitting` for ~30 min *after* Apple had already marked the build `VALID`; the poller, not the upload, is what hangs. Separately, 1.2.0 still holds build 55 in `WAITING_FOR_REVIEW` and is deliberately untouched — a build sitting on TestFlight cannot change what Apple is reviewing, so submitting 58 was safe, but swapping 1.2.0's build is not. **Behaviour UNVERIFIED** — still no device QA on any SDK 57 binary, and that is now the only thing between this and a release |
| iOS | **build 57** (2026-08-17) | `25e953e95c7925010c0fad214d433b8c0c70a65f` | **read from the `.ipa`** | current `main`, 1.2.0 — **the first Expo SDK 57 iOS binary** (RN 0.86.2, React 19.2). Built locally on `ignia-mac`, verifier green on all four targets. **`IN_BETA_TESTING` externally**: added to *Public Beta Testers* and beta review came back `APPROVED` immediately, since beta approval carries within an already-reviewed version. **Its runtime is its own** — the SDK bump moved it off builds 50–55's `886bf0b3…`, so no OTA crosses in either direction. **And no OTA reaches build 57 either, as of 2026-08-17** — measured 2026-08-18, gate `b3c50e91…` against `25e953e9…` read out of the `.ipa`. The cause is the same `plugins/withGradleJvmArgs.js` change already noted on the vc 33 row: it is an **Android-only** plugin, the plugins array is hashed as part of `app.json`, and it moved BOTH hashes. That consequence was recorded for Android and not for iOS, so the iOS channel read as open when it had been shut for a day. **Both platforms now need a binary before any JS fix reaches a tester.** **Build 56 does not exist**: the first attempt was killed by a closing SSH session (`BUILD INTERRUPTED`) and `autoIncrement` burns a number per attempt. Does **not** disturb the 1.2.0 submission, which still holds build 55 and `WAITING_FOR_REVIEW`. **Behaviour UNVERIFIED** — no device QA on any SDK 57 binary |
| Android | **vc 32** (2026-08-17) | `d8741525dbf428d6e994ec120108e333e385eab9` | **read from the `.aab`** | branch `chore/expo-sdk-57`, alpha, 1.2.0 — **the first Expo SDK 57 binary** (RN 0.86.2, React 19.2). Built and signed on the Windows workstation, verifier green (signer `CN=Macro Log Dev`, `expo-channel-name` production, both quick-add services present). `BUILD SUCCESSFUL in 19m18s`. **Its runtime is its own** — an SDK bump moves the hash, so no OTA reaches vc 30/31 from here or vice versa, and the Android fleet now spans three runtimes. **NOT built from `main`**: the SDK 57 branch is deliberately unmerged while iOS 1.2.0 is in review. **Behaviour UNVERIFIED** — no device QA, and the check that matters most is **Google Sign-In on a Play install**, broken twice historically and structurally invisible on a local build |
| iOS | **build 55** (2026-08-15) | `886bf0b3d384b8a43477730cdd0124572b954711` | **read from the `.ipa`** | current `main`, TestFlight `VALID`, 1.2.0 — **the App Store candidate: attached to 1.2.0, `WAITING_FOR_REVIEW`, manual release.** It exists because **build 54 was the wrong binary and the artifact proved it**: 54 was cut from `ea96f48` and the verification-email fix landed in `86183368` after, so 54's `main.jsbundle` held **zero** `sendVerificationEmail` against one `sendPasswordReset`. 55 holds `sendVerificationEmail` 1 / `sendOwnedVerificationEmail` 1. **Grep the bundle, not the commit log** — a build is only as current as the JS baked into it, and `git log` on the Mac cannot tell you what a finished `.ipa` contains. Same fingerprint as 50–54 (JS-only), so no OTA is stranded either way |
| iOS | build 54 (2026-08-15) | `886bf0b3d384b8a43477730cdd0124572b954711` | **read from the `.ipa`** | **superseded and NOT shipped** — swapped out of the 1.2.0 submission the same day for predating the verification-email fix (see build 55). Structurally green; it was the *content* that was wrong, which no verifier checks | current `main` (`ea96f48`), TestFlight `VALID`, 1.2.0 — **the App Store candidate: `WAITING_FOR_REVIEW`, manual release.** Contains no code change over build 53; it exists because 53's *embedded* JS predates the TDEE discontinuity fix (`866369ba`, merged 8 h after 53 was uploaded), so 53 carries that fix only over the air and a fresh App Store install would run the 1,700-kcal bug for one session. **Fingerprint identical to 50–53** — JS-only, so the 08-14 OTA already reaches it and no OTA is stranded by it. Internal group only; 53 remains the external TestFlight build |
| iOS | **build 53** (2026-08-14) | `886bf0b3d384b8a43477730cdd0124572b954711` | **read from the `.ipa`** | current `main`, TestFlight, 1.2.0, **`IN_BETA_TESTING` externally — the build every tester should be on.** **`transferCurrentComplicationUserInfo` does NOT wake a WidgetKit complication** — it wakes a ClockKit one, and this complication has never been ClockKit. Apple FB12926788, open since 2023-08. That ends the real-time-to-the-wrist goal; the replacement is a PULL, `WKApplicationDelegate` + `scheduleBackgroundRefresh` (~hourly for an app in the dock), reading `receivedApplicationContext`, which holds the phone's latest value whether or not any wake happened. Staleness bounded at ~1h instead of "until you open the watch app". The SwiftUI `.backgroundTask(.watchConnectivity)` modifier was removed with it — `handle(_:)` owns every background task now, and two owners means `setTaskCompletedWithSnapshot` twice, a crash on a background wake. Also drops `@MainActor` from `LogQuickAddSlotIntent.perform()`, which was queueing the widget's redraw behind React Native's startup, and records `perform()` duration + warm/cold to the App Group. **Behaviour UNVERIFIED** |
| iOS | build 52 (2026-08-14) | `886bf0b3d384b8a43477730cdd0124572b954711` | **read from the `.ipa`** | superseded by 53, TestFlight, 1.2.0 — **the widget quick-add stops waiting on the network.** `applyOptimistically` ran *after* both round trips despite its name, and since build 50 also behind `assertToWatch`'s 3s activation wait. The write path is now `stage` (synchronous: bump the snapshot, request the reload, park the row) → `commit` (network, unpark, watch push last). The widget intent uses `logDeferred` and returns in milliseconds, which matters because a `reloadTimelines` requested from inside `perform()` is deferred (FB11522170) while the reload the system performs **on return** is the reliable one. The park moved *before* the attempt — a write-ahead log — so returning early cannot lose a row: `flushPendingLogs` replays it under the same id, an idempotent overwrite. Carries builds 50 and 51 in full. **Same fingerprint as 50/51** — `targets/`-only Swift |
| iOS | **build 51** (2026-08-14) | `886bf0b3d384b8a43477730cdd0124572b954711` | **read from the `.ipa`** | TestFlight `VALID`, 1.2.0 — **the watch stopped spending its reload budget on bytes it already had.** `store()` wrote and called `reloadAllTimelines()` unconditionally: `start()` on every launch, and `ingest()` **twice per background wake**. That is metered at 40–75/day, and once spent WidgetKit throttles and the face stops moving — indistinguishable from a delivery that never arrived. Deduped now, matching what the phone has done since 2026-08-10. Adds watch-side instrumentation: which callback stored (`userInfo` can only come from `transferCurrentComplicationUserInfo`, so it proves the wake) and when the complication last built a timeline, both on the mirror screen. **Fingerprint IDENTICAL to build 50** — the change is Swift under `targets/` only, which is the documented rule holding again |
| iOS | build 50 (2026-08-14) | `886bf0b3d384b8a43477730cdd0124572b954711` | **read from the `.ipa`** | superseded by 51/52, TestFlight `VALID`, 1.2.0, **internal group only** — **the watch real-time push (ADR-0023)**. A meal logged with the app closed now reaches the wrist: the assert parks its envelope and waits for `WCSession` activation instead of dropping it, `WatchLinkSession` drains that park on `activationDidCompleteWith`, and the widget chip's intent conforms to `LiveActivityIntent` so it runs in the app's process where a session exists. **Its runtime is its OWN** — `modules/watch-link/ios` changed, and that directory IS hashed, so no OTA reaches build 49 from here and vice versa. Verifier green on all four targets; `systemProtocols: SessionStarting` is present on `LogQuickAddSlotIntent` and on no other intent, which is the `LiveActivityIntent` conformance surviving extraction into **both** the app's and the appex's metadata. **Behaviour UNVERIFIED** — no complication has been watched to move after a meal logged outside the app |
| iOS | build 49 (2026-08-13) | `1e9840b69350067e4927bc698fd497d1aa5167d4` | **read from the `.ipa`** | superseded by 50, TestFlight, 1.2.0, `IN_BETA_TESTING`. Wide face gains the progress bars, the chip column stops clipping long preset names, and quick-add chips are readable on **tinted/clear Home Screens**: those modes render `.accented`, flattening every colour into one tint, so a filled capsule and its label resolved to the same colour and the text vanished into its own background. Verified from device screenshots — the chips drew as solid blobs. Stroked instead of filled whenever rendering is not full colour. **Shares 46's runtime**, which is the `targets/`-Swift rule holding across builds 46→49. Builds 47 and 48 exist: 47 removed a `ForEach` over duplicate identities that crashed the wide face, 48 was superseded before submission |
| Android | **vc 30** (2026-08-13) | `6519916642c291db8255d433bf651e26c66a28b4` | **read from the `.aab`** | current `main`, alpha, 1.2.0 — **N4b, the wide widget face**. `resizeMode` was absent from the generated provider XML, which Android defaults to `none`, so the launcher would not let anyone drag the widget wider; it is now `horizontal\|vertical`, and `widgetInfo.width` (always available in the task handler, never read) drives a row layout at ≥220dp. Verifier green: signer `CN=Macro Log Dev`, `expo-channel-name` production, both quick-add services survived the manifest merge. versionCode read from Play, **not** from the build log |
| iOS | **build 46** (2026-08-13) | `1e9840b69350067e4927bc698fd497d1aa5167d4` | **read from the `.ipa`** | current `main`, TestFlight, 1.2.0 — same change as vc 30: `.systemMedium` plus a purpose-built `HomeWideView`. **Its fingerprint moved off build 44/45's `3752c757…` because `app.json` is hashed as a whole** — the Android-only `resizeMode` key moved the *iOS* hash too, the documented trap running in the other direction. So no OTA reaches 44/45 from here, and this cohort split is real rather than a Windows/Mac artefact. Verifier green on all four targets. **Behaviour UNVERIFIED** — no wide widget has been placed on a home screen |
| iOS | **build 45** (2026-08-11) | `3752c757fc2882fa432c09d36715d9cd1bb61911` | **read from the `.ipa`** | current `main`, TestFlight, 1.2.0 — adds the **Siri** quick-add watch push. **Its fingerprint is IDENTICAL to build 44's**, which is the `targets/`-Swift rule holding: the only change is Swift under `_shared/`, so one `eas update` reaches 44 and 45 together. Verifier green on all four targets. **Behaviour UNVERIFIED** — no spoken quick-add has been watched to move a complication |
| iOS | **build 44** (2026-08-10) | `3752c757fc2882fa432c09d36715d9cd1bb61911` | **read from the `.ipa`** | current `main`, TestFlight, 1.2.0 — **the watch complication fix**: the phone now asserts over both WatchConnectivity queues and the watch app finally implements `didReceiveUserInfo`, which is where `transferCurrentComplicationUserInfo` lands. Built locally, verifier green on all four targets. **Its runtime is its OWN** — see the `modules/` rule below; an OTA aimed at build 41 does not reach it and vice versa. Builds 42 and 43 do not exist (a cloud attempt that died in *Configure expo-updates*, and a local one killed by the deleted watchOS runtime). **Behaviour is UNVERIFIED**: no complication has been watched to refresh on a wrist |
| Android | **vc 29** (2026-08-09) | `ca2dc124281f48e63c17111214f0ee8dfdc34784` | **read from the `.aab`** | current `main`, alpha, 1.2.0 — same commit as iOS build 41 (the `ignia://` scheme restore moved BOTH fingerprints, since `app.json` is hashed whole). vc 28 does not exist: that attempt died on `No space left on device` mid-CMake — the disk was carrying iOS build 41's DerivedData at the time; clear it before an Android build |
| iOS | **build 41** (2026-08-09) | `1d89fedf59c7975b06c43762cc6414547845c9fe` | **read from the `.ipa`** | current `main`, TestFlight, 1.2.0 — **restores the `ignia://` URL scheme**, unregistered since build 29's CFBundleURLTypes "de-duplication", which left the widget's face tap opening nothing on builds 29–40. Verified by `verify-mobile-artifact.mjs`, which now requires both schemes with whole-line matching |
| iOS | build 40 (2026-08-09) | `c9abb3165ad1c381a0607f98e1886b267aab1ba3` | **read from the `.ipa`** | current `main`, TestFlight, 1.2.0 — the dictation mic + the R1 sheet redesign. **Builds 38 and 39 are on TestFlight but MUST NOT be tested**: both lack `NSMicrophoneUsageDescription` (expo-camera's `microphonePermission: false` DELETES the key another plugin wrote), so the first mic tap crashes them. 39 was submitted before being verified — the mistake `scripts/verify-mobile-artifact.mjs` now makes structurally impossible |
| Android | vc 27 (2026-08-09) | `f1786d8e7087437f23670cf3dbad03302f50c276` | **read from the `.aab`** | current `main`, alpha, 1.2.0 — same content as build 40. Emulator-verified: full regression sweep 15/15 screens |
| iOS | build 37 (2026-08-08) | `84dafba371a083476d795b1e7408a51d1d6fb59b` | **read from the `.ipa`** | current `main`, TestFlight, **version 1.2.0**. Fixes the widget quick-add button (extension-process keychain, ADR-0020 amendment), adds the quick-add outbox and es-PR Siri phrases. Builds 33–36 do not exist — four attempts at one config plugin |
| iOS | build 31 (2026-08-08) | `fb7398e51984ecb39f1bb411329ef293fc96e095` | **read from the `.ipa`** | current `main`, TestFlight. **Behaviourally identical to build 30 on iOS** — it exists only to re-align the iOS runtime after `plugins/withGradleJvmArgs.js`, an Android-only plugin, moved the iOS hash. That is the `app.json` rule below, in the other direction. Carries N3, still **UNVERIFIED** |
| iOS | build 30 (2026-08-08) | `249ba992975203bb91c23aaddd5bab9d54d034fe` | **read from the `.ipa`** | superseded by 31. First binary with the fasting Live Activity (N3, ADR-0021), built off branch `n3-live-activity`. `NSSupportsLiveActivities` in `app.json` is what moved its fingerprint; the Swift that matters never would have |
| Android | vc 26 (2026-08-08) | `79e92d26082607ab50b8836f2e70155cfc090c4d` | **read from the `.aab`** | current `main`, alpha — carries the offline quick-add fix. An `eas update` is published against this runtime |
| Android | vc 25 (2026-08-08) | `7a0e4edf389423589c41b692db34e34984ea22cc` | **read from the `.aab`** | current `main`, alpha — version **1.2.0**. The first Android binary anything has ever launched: emulator-verified (`.maestro/README.md`) |
| Android | vc 24 (2026-08-08) | `01edf656412ecaab3a16363617f6f7f642fbe535` | **read from the `.aab`** | current `main`, alpha — carries `plugins/withGradleJvmArgs.js`, and **proved it**: built with no heap flags in `GRADLE_OPTS` and 0 OOMs. vc 22/23 do not exist (wrapper missing `JAVA_HOME`, then `ANDROID_HOME`) |
| Android | vc 21 (2026-08-08) | `f101b1d4e942c2ad032786b27821807ee3cfb6cd` | **read from the `.aab`** | superseded within the hour — N3's `NSSupportsLiveActivities` moved the **Android** hash, which is the measurement in the `app.json` note below. Its versionCode was 21, not the 20 the plan expected |
| iOS | build 29 (2026-08-08) | `d0487ca7bfb2e7ac64ad12e03a88d452ed51ce9d` | **read from the `.ipa`** | superseded by 30, TestFlight — adds spoken preset names ("log overnight oats in Ignia"). **Its runtime is its own**: deleting the inert `EXPO_PUBLIC_FEATURE_PHOTO_SCAN` key moved it off `4734a4b6…`, because `eas.json` IS hashed even though Swift is not |
| iOS | build 28 (2026-08-08) | `4734a4b6ae3cb652db2a4f920ee0b7ed8c073429` | **read from the `.ipa`** | superseded by 29. The build that made Siri work at all, and **the first iOS binary whose write path was exercised on hardware** — a Siri phrase logged a real row. Shares 27's runtime, so one `eas update` reaches both |
| iOS | build 27 (2026-08-07) | `4734a4b6ae3cb652db2a4f920ee0b7ed8c073429` | **read from the `.ipa`** | superseded. **Its Siri half never registered** — see the required-parameter trap above. Identical fingerprint to 28 because the fix was Swift-only |
| Android | vc 18 (2026-08-07) | `cc3da8b9a22df7180c55e6cab5cd8decccdb98bb` | **read from the `.aab`** | superseded by vc 21; carries the Quick Settings tile |
| iOS | build 25 (2026-08-07) | `6c756c19b3e35948b85e42a3b337eec588128d3c` | **read from the `.ipa`** | superseded by 27; still what un-updated testers run |
| iOS | build 24 | `781be0c885005e1d02bcf41408988c6622ff222e` | Windows `fingerprint:generate` — **unverified** | **THE LIVE APP STORE BINARY** (1.1.0, `READY_FOR_SALE` since 2026-08-08) until 1.2.0/build 54 is approved and released. Every member of the public runs this. Its runtime is a Windows-generated hash nobody has confirmed, so **no OTA should be assumed to reach it** — the public has been reachable only by a store release since 08-08 |
| Android | vc 13 (2026-08-07) | `5758fe4f232d5e6fe1ca369299512cfec0d39e13` | **read from the `.aab`** | superseded by vc 18; still what un-updated testers run |
| Android | vc 11 | `c0b85c15e6631d99e8ccef61867d937389094ae6` | Windows `fingerprint:generate` — **unverified** | superseded |

**A build log's versionCode line is not evidence either.** vc 18's build printed
*"Incrementing versionCode from 18 to 19"* and `[CONFIGURE_ANDROID_VERSION]
Version code: 19`, and the artifact it produced is **versionCode 18** — proven by
`androidpublisher`'s `bundles` list, whose `sha256` for vc 18 matches the local
`.aab` byte for byte. The log line describes the *remote counter* being advanced
for the next build, not what got baked in. So the same rule that applies to
fingerprints applies here: read it from the artifact or from Play, never from the
build output.

Also note `autoIncrement` burns a number per **attempt**: vc 14–17 do not exist,
consumed by three Gradle failures (unset `ANDROID_HOME`, a missing
`com.facebook.react` classpath entry in the new local module, and a Kotlin static
resolved through the wrong class). iOS **build 26** does not exist either, for the
same reason — one Swift failure, described below.

**iOS build 43 does not exist** (2026-08-10). A **local** build on `ignia-mac`,
killed at *Run fastlane* by `watchOS 26.5 must be installed in order to archive
the scheme` — the watchOS simulator runtime had been deleted hours earlier during
a disk sweep, on the wrong reasoning that the SDK surviving in Xcode.app was
enough. The scheme embeds `IgniaWatch.app` and archiving needs the platform.
`DEV_ENVIRONMENT.md` §3.10 now carries the corrected rule and the two checks that
falsely look authoritative.

**iOS build 42 does not exist** (2026-08-10). An EAS **cloud** build, queued only
because `ignia-mac` was offline, errored ~2 minutes in at *Configure expo-updates*
— before any Swift compiled, so it is evidence about the cloud worker and not
about the commit. Two things worth keeping from it: the `EXPO_ASC_*` override in
`CLAUDE.local.md` **works** (all four targets printed *All credentials are ready
to build*, no 401), and the build reported runtime version `721f39e507ba…`, which
is neither the Mac's `1d89fedf…` nor the Windows workstation's — **the EAS worker
is a third machine with a third fingerprint**, so a cloud artifact would have
shipped under a runtime no installed binary matches even had it succeeded. Build
locally.

**An App Shortcut may not carry a REQUIRED parameter, and breaking that rule
registers nothing at all.** iOS validates `AppShortcutsProvider` on the device at
registration time, reports nothing when it fails, and one invalid shortcut
invalidates the whole provider — so the app simply never appears in the Shortcuts
app and every phrase answers *"I can't help with that"*.

**Nothing upstream catches it.** The build is clean, there is no warning, and
`Metadata.appintents` still extracts perfectly into the binary — build 27's
archive holds a flawless `autoShortcutProviderMangledName`, both `autoShortcuts`,
all three intents and their phrase templates. Verifying the metadata is present
therefore proves nothing about whether Siri will ever see it; that check passed on
the binary that shipped broken.

The rules, from [WWDC22 *Implement App Shortcuts with App
Intents*](https://developer.apple.com/videos/play/wwdc2022/10170/): parameters
"should be defined as optional so the app can gracefully handle cases where users
don't specify them in the initial phrase", and they "are not meant for open-ended
values". Build 27 broke both — `LogPresetIntent.preset` was required and named in
no phrase, `LogMacrosIntent.calories` was required *and* an arbitrary number.

Declaring them optional costs nothing: disambiguate against `suggestedEntities()`,
or demand the value with `requestValue`, and the domain invariant is enforced in
`perform()` where it belongs rather than in the declaration.

**Swift block comments NEST, and it cost a build.** `/**` … `*/` nests, and
backticks mean nothing to the lexer, so a literal `_shared/` + `*` written inside
a doc comment opens a nested comment that never closes. It is reported as
`unterminated '/*' comment` against the **last brace in the file**, hundreds of
lines from the cause, and it cascades into `cannot find <Type> in scope` errors in
every other target. This is exactly why `Glance.swift` explains the same glob in
`//` line comments — those do not nest. Do not write that glob inside a block
comment.

**Only the rows marked "read from the artifact" are evidence.** Build 24 and vc 11
are Windows-generated hashes recorded before the machine-dependence above was
known, and their artifacts have since been overwritten on the Mac, so they cannot
be checked. Treat them as unknown, not as fact: since the Windows/Mac divergence
is commit-independent, they most likely carry Mac values nobody recorded. If it ever matters, rebuild at that commit on the Mac and read the
artifact — do not re-derive on Windows.

That also means the earlier claim that **the fleet spans two runtime versions per
platform** is not established. It was the Windows number for one binary compared
against the Mac number for another. A genuine split is still possible — two
binaries built from different commits normally *do* differ — but publishing
twice "to cover both" is only worth doing once both halves have been read out of
their artifacts. Otherwise the second publish targets a runtime nobody runs.

**2026-08-09: the gate was run at commit `43f15149` and both platforms matched
their live artifacts exactly** — android `ca2dc124…` (vc 29), ios `1d89fedf…`
(build 41) — so the OTA published that day lands on the current cohort. One
precondition was recorded here as load-bearing and **turned out not to be**: the
Mac had an `apps/mobile/android/` prebuild dir at the time (created to build a
QA APK for the Maestro emulator), and it was deleted before generating on the
belief that `dir:android` is hashed. It is not — see the struck-through bullet
above, measured 2026-08-17. Deleting it changed nothing; the gate would have
returned the same hash either way. Harmless as a habit, but do not reach for it
as an explanation when a fingerprint fails to match, and do not spend a step on
it. Nor on `node_modules`, disproven the same day: the only surviving repo-state
cause is CRLF-vs-LF in two files, and the rest is `@expo/fingerprint` behaving
differently per OS — see the bullets above.

Do not assume "published" means "delivered"; check which runtime version the
update went out under:

```sh
npx eas update:list --branch production --limit 3   # prints the runtime version
```

Both new binaries were built locally on `ignia-mac` at **zero EAS quota**
(`build-ios` / `build-android` skills, `DEV_ENVIRONMENT.md` §3.10–3.11).

**Changes the fingerprint** (⇒ needs a build): any dependency carrying native
code, native config in `app.json` (permissions, icons, splash, plugins,
entitlements), an Expo SDK upgrade. A pure-JS dependency usually does not — but
"usually" is why you run the command instead of reasoning about it.

**`modules/*/ios` IS hashed, even though `targets/` is not — measured 2026-08-10.**
The two look interchangeable and are not. An Expo Module is a **directory source**
in the fingerprint (`modules/watch-link/ios`, `modules/quick-add-credentials/ios`
and `modules/fasting-live-activity/ios` all appear in `sources` as `dir:` entries),
so editing one line of Swift there moves the runtime version. An apple-target under
`targets/` does not. Build 44 changed Swift in **both** places and came out on
`3752c757…`, off build 41's `1d89fedf…` — so the OTA published earlier that day
against `1d89fedf…` does not reach it. Read the sources when in doubt:

```sh
npx expo-updates fingerprint:generate --platform ios \
  | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>console.log(JSON.parse(s).sources.filter(x=>x.type==='dir').map(x=>x.filePath).join('\n')))"
```

**Swift and Kotlin under `targets/` do NOT change it, and that inverts the gate.**
Measured 2026-08-08: `QuickAddIntents.swift` was edited and iOS build **28** came
out carrying the change — `isOptional` flipped in the shipped
`Metadata.appintents` — while the fingerprint stayed `4734a4b6…`, byte-identical
to build 27's. Confirmed three ways: both `.ipa`s and a fresh
`fingerprint:generate`. This line previously claimed the opposite.

So for a native-source change the gate gives the **wrong** answer: an unchanged
hash normally reads as "ship it over the air", and here that publishes an update
containing no Swift at all — landing successfully, reporting success, and fixing
nothing. **The gate answers "will an OTA reach these binaries", never "is an OTA
sufficient".** Only the second question matters once you have touched native
source, and the hash cannot answer it: if the change is under `targets/` or
`modules/`, it needs a build no matter what the gate says.

The upside of the same fact: builds 27 and 28 share runtime `4734a4b6…`, so a
single `eas update` reaches both cohorts.

**`app.json` is hashed as a WHOLE, so a key for one platform moves the other
platform's fingerprint too.** Measured 2026-08-08: adding `NSSupportsLiveActivities`
under `ios.infoPlist` — a key Android cannot read and no Android file mentions —
moved the **Android** hash to `5d911df8…`, off live vc 21's `f101b1d4…`. vc 21
existed specifically to end Android's OTA stranding, and it was re-stranded about
forty minutes later by an iOS-only line. **Generate BOTH platforms after any
`app.json` edit**, not just the one you meant to change. The same applies to the
`plugins` array, including `./plugins/*` — `withGradleJvmArgs.js` is an
Android-only plugin and it moves the iOS hash.

**It happened again on 2026-08-25, and this time the fix is structural.**
`dd3ceb10` added ONE line to `app.json` — `android.permission.health.READ_EXERCISE`,
a Health Connect permission iOS cannot read and no iOS file mentions — and it
moved the **iOS** runtime `7b347b0f…` → `f319b32d…`, off **build 60, the public
App Store binary**. Both channels were shut for most of a day and the handoff
written that morning recorded the iOS one as open, because nobody re-gated after
the commit. Identified by measurement rather than by reasoning: deleting the
single line on `ignia-mac` returned the hash to `7b347b0f…` exactly.

**So Android-only permissions no longer live in `app.json`.** `cf260f07` moved
`READ_EXERCISE` to **step 4b of `patch-android-release.mjs`**, which writes the
gitignored `android/app/src/main/AndroidManifest.xml` — the same file and the
same reasoning as the EAS Update channel in step 4, and the same rehoming
`4ec7d2d7` applied to the ABI set. A `dir:` source hashes only git-TRACKED
content, so nothing written under `android/` can move either runtime.

Two consequences worth knowing before adding a permission:

- **Add an Android-only permission to step 4b, never to `app.json`.** A
  permission BOTH platforms need still belongs in `app.json` — the trap is
  one-sided keys, not permissions.
- **The rehoming costs the only upstream check there was.** Prebuild output is
  gitignored, `tsc` cannot see a manifest, and a permission that silently
  vanishes reads at runtime as "the ring reported nothing". So
  `scripts/native-expectations.json` grows `requiredPermissions` and
  `verify-mobile-artifact.mjs` fails a build whose shipped `.aab` lacks one.

**`eas.json` is hashed too, and a submit profile is enough.** Measured the same
day while looking for a way past a Play block: adding one `submit` profile moved
the Android hash `b0849308…` → `4ea6df02…`. Reverted within the minute. Do not
edit `eas.json` between a build and its OTAs.

**Does NOT change it** (⇒ ships over the air): `.ts`/`.tsx`/`.js` source, UI,
styles, business logic, i18n strings, Metro-bundled assets.

Two more things worth knowing: testers get an update on the **next** launch, not
the current one (it downloads in the background and applies on the following
start), and a bad update is undone with `eas update:roll-back-to-embedded`, which
returns everyone to the JS baked into their binary.

See the `build-android` skill for the full decision.

# Telling users an update exists — two mechanisms, one banner

`UpdateBanner` on Today is the only surface that tells a user their app is
stale. It covers both mechanisms, and they fail in opposite ways:

| Case | Source of truth | What the user does |
|---|---|---|
| **OTA** (JS only) | `expo-updates` — bundle already downloaded | one tap, reloads in place |
| **Binary** (native) | `public/app-version.json` on the hosting site | leaves for the store |

The OTA half is self-maintaining: `expo-updates` knows a bundle is pending and
the banner clears itself when tapped.

**The binary half cannot maintain itself, and that is its whole risk.** If
`latestVersionCode` lags what Play ships there is no error, no warning, and no
visible difference from a working feature: every install just keeps believing it
is current. Two things cover it, and neither is your memory:

```sh
node scripts/app-version-sync.mjs           # derive it from the live Play tracks
node scripts/app-version-sync.mjs --check    # report drift, change nothing
```

`npm run doctor` runs the `--check` path (*app-version.json matches what Play
ships*) and **fails** on drift, naming the fix. Never hand-edit the number —
`androidpublisher` is the authority, the same one the signing-cert check uses.
Deploying is still a separate act: `firebase deploy --only hosting`, or the
corrected file reaches nobody.

`ios.latestBuild` is `0`, which disables the iOS prompt on purpose. TestFlight
builds run ahead of the App Store, so pointing a store user at a build they
cannot install is worse than saying nothing. Set it to the **live App Store
build** — never the TestFlight one — if the iOS prompt is ever wanted.

# This app is in production

Treat every change here as a production change: this app is on the iOS App Store.

**Do not read a version number out of `app.json` — it is not evidence of what
shipped.** Which version is live, which build backs it, and what is merged but
not yet in any binary all live in **`STATUS.md`**, which carries the command to
re-check each one. Nothing in this folder should restate them; a second copy is
how "planned" and "shipped" became indistinguishable here before.

## A hashed file's LINE ENDINGS can move the fingerprint with no commit at all

**Measured 2026-08-17, and it stranded a shipped binary.** `apps/mobile/.gitignore`
is a `file:` fingerprint source *and* one of the two files this repo checks out as
CRLF on Windows. vc 32 was built while it was CRLF, so the `.aab` embeds
`d8741525…`. A later `git commit` normalized it to LF — `.gitattributes` says
`* text=auto eol=lf`, and git had been warning *"CRLF will be replaced by LF the
next time Git touches it"* — and the Android fingerprint moved to `0c82dbc1…`.

Proven by flipping only that file's line endings back and forth:

| `apps/mobile/.gitignore` | android fingerprint |
|---|---|
| CRLF (48 lines) | `d8741525…` — what vc 32 ships |
| LF (55 lines) | `0c82dbc1…` — the normalized, correct tree |

Everything else was ruled out first by measurement: the JS change itself (stash
in/out, identical hash), root `engines`, the `android/` prebuild dir, and the 39
Gradle output directories the build writes inside `node_modules/*/android`. Even
checking out the exact build commit `c75352ef` gave `0c82dbc1…`, which is what
proves the drift is worktree state and not repo content.

**Three consequences.**

1. **`git status` stays clean through this.** Nothing signals the drift — the
   index was always LF; only the worktree changed. A gate run days after a build
   can silently disagree with the binary for no reason a diff will show.
2. **vc 32 is an ORPHAN runtime.** Its hash is only reproducible from a CRLF
   worktree, which is a state `.gitattributes` actively removes. **No OTA from a
   correct tree can ever reach vc 32** — do not try to recreate the CRLF to
   publish one; the next git operation undoes it. It needs a replacement build.
3. **iOS is unaffected.** Build 57 was produced on `ignia-mac`, where the checkout
   is LF already, so its `25e953e9…` was computed from the same normalized state
   the tree is in now.

The durable fix is that the worktree now matches the index. Re-normalizing is a
one-time event; once every hashed file is LF the class is closed. If a
fingerprint ever fails to match for no visible reason, **check line endings on
`.gitignore` and `targets/widget/expo-target.config.js` before anything else.**
